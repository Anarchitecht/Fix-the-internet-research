# Architecture

Written only after `evidence.md` is complete and the contradiction pass has run once. Empty until
then, deliberately: a synthesis written against a partial evidence file selects components on
whatever happened to be retrieved first.
