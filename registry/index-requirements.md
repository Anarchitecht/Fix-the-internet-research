# Requirements index

What each mechanism needs from the rest of the system. The composition check runs on this: a conflict exists when one selected component removes something another selected component requires.

| Key | Dom | Title | Requires from elsewhere |
|---|---|---|---|
| `ADAMS-ARXIV-25` | B | DistributedANN: Efficient Scaling of a Single DiskANN Graph Across Thousands of Computers | The system requires a distributed key-value store with per-key durability and availability properties adequate for serving graph-node reads under load; the paper explicitly relies on this being a well-understood, separately operated component (citing Amazon Dynamo) rather than building its own storage layer. The reliability results assume node-scoring requests can be safely timed out under tail la |
| `ADAR-FM-00` | I | Free Riding on Gnutella | None as a positive mechanism — this paper reports a measurement, not a protocol. Any component that assumes participants will act as content or bandwidth providers by default must budget for a request-serving population concentrated in a small fraction of participants: this trace shows the top 1% of hosts answering roughly half of query traffic and the top 20% holding roughly all shared file volum |
| `ADLER-ARXIV-24` | E | Personhood Credentials: Artificial Intelligence and the Value of Privacy-Preserving Tools  | The credential-limit check (one credential per person per issuer) requires the issuer to have access, at enrollment time, to some external one-per-person-bounded signal: an identity document system with its own finite-per-person guarantee, a biometric-matching system capable of comparing a new enrollee against every prior enrollee's stored (or hashed/encrypted) biometric template, or a pre-existin |
| `AGARWAL-ICWSM-24` | K | Decentralised Moderation for Interoperable Social Networks: A Conversation-Based Approach  | A cross-instance conversation-reconstruction mechanism, as used here, requires every reply-bearing post to carry a globally unique, instance-independent identifier (this paper uses the toot's URL) because per-instance database identifiers for a toot and its parent are not consistent across the instances that hold copies of the same conversation. A Model Sharing moderation-federation strategy requi |
| `AIYER-SOSP-05` | I | BAR Fault Tolerance for Cooperative Services | A trusted admission authority must exist to issue each participant exactly one cryptographic public-key identity and to control who may join; the protocol's entire accountability mechanism, and therefore its incentive-compatibility guarantee for rational nodes, depends on this identity being both unique per participant and expensive to obtain more than once. Message delivery must be synchronous, w |
| `ALBRECHT-CTRSA-21` | H | Mesh Messaging in Large-Scale Protests: Breaking Bridgefy | - A mesh-messaging deployment that wants to prevent this class of attack needs, at minimum, an authenticated key-establishment/handshake mechanism (the handshake here is unauthenticated and stateless, which is what enables both the impersonation and MITM attacks) and message authentication on ciphertexts (there is none here, which is what enables the chosen-ciphertext padding-oracle attack). - Any |
| `ALBRECHT-SP-23` | H | Practically-exploitable Cryptographic Vulnerabilities in Matrix | - A federated group-messaging design in which a server (homeserver) controls room membership and device-list metadata needs that metadata itself to be cryptographically authenticated to end-to-end-encrypted-room participants, or a malicious server can silently insert an eavesdropping device without breaking any per-message cryptography — the paper's vulnerability 1 shows the absence of that authen |
| `ALMEIDA-CSUR-25` | D | Approaches to Conflict-free Replicated Data Types | Operation-based CRDTs and, more strictly, pure operation-based CRDTs require the messaging layer to supply causal broadcast: exactly-once, causally-ordered delivery to every replica. Pure operation-based CRDTs additionally require the messaging middleware to expose causal timestamps and per-node causal-stability notifications to the CRDT logic (Tagged Causal Broadcast), which is a stronger interfa |
| `ALVISI-SP-13` | F | SoK: The Evolution of Sybil Defense via Social Networks | ACL requires each honest verifying node to hold, or be able to sample, a local view of the social graph reachable by random walk from itself; it does not require a global, centrally computed graph snapshot at query time, but the paper's own experiments compute walks over a fully materialized graph. ACL's guarantee (Theorem 3) is conditioned on the target subset S having a power-law degree distribu |
| `ALWEN-CRYPTO-20` | H | Security Analysis and Improvements for the IETF MLS Standard for Group Messaging | - Every group member must retain, for each node on its own direct path, the most recently generated private key, because processing future updates whose co-paths overlap that path requires it; a system layer that tries to erase keys more aggressively than TreeKEM's own update logic requires would break the protocol's own correctness, not just its forward secrecy. - The delivery mechanism (an untru |
| `ALWEN-CRYPTO-23` | H | Fork-Resilient Continuous Group Key Agreement | - Any fork-resolution procedure built on FR-CGKA needs the underlying transport to guarantee only causality-respecting delivery per sender-receiver pair (packets from the same origin to the same destination arrive in the order sent) — the paper states an otherwise-unreliable network satisfying only this property already suffices to deliver packets in "arbitrary but causality respecting order," whi |
| `ALWEN-EUROCRYPT-22` | H | CoCoA: Concurrent Continuous Group Key Agreement | - CoCoA still requires a stateful, synchronized-round delivery mechanism: the untrusted server "works in rounds, collects operations into batches and sends them out at the end of each round" (the paper's own formal definition, Section 2.1) — this is the same total-ordering-of-delivered-messages structure TreeKEM/MLS depends on, only batched rather than one-message-at-a-time; a fully asynchronous,  |
| `ALWEN-EUROSP-17` | I | Towards Practical Attacks on Argon2i and Balloon Hashing | A system choosing Argon2i-B (or another data-independent MHF) as a password-hashing or key-stretching primitive must select the pass count τ knowing that quality-2 or better memory-reduction attacks exist against low pass counts at practical memory sizes (e.g., τ=6 at 1GB), so the choice of τ trades directly against user-perceived latency: the paper states that increasing τ multiplies running time |
| `ALWEN-SCN-24` | H | DeCAF: Decentralizable CGKA with Fast Healing | DeCAF requires an append-only, agreed-upon transcript of protocol messages available to every group member — instantiated in the paper as a blockchain, but the authors state a single central server suffices structurally, at the price of losing the properties below. The security proof requires an IND-CPA-secure skuPKE scheme (a concrete instantiation from the computational Diffie-Hellman assumption |
| `ALWEN-TCC-20` | H | Continuous Group Key Agreement with Active Security | Security proofs are conditioned on a public-key infrastructure (PKI) functionality (F_pki) that is assumed idealized: correctly delivering the actual public key of the party requested, with no verification available to a joining party that its own welcome message came from a legitimate sender. Two of the three constructions (P-Pas, P-Act) are proved secure only in the random-oracle model, requirin |
| `AMBATI-SACMAT-26` | E | SoK: Self-Sovereign Digital Identities | A credential-based identity-binding approach requires an external, already-trusted issuing authority (a government, university, or employer) willing to attest off-protocol that a given DID corresponds to a specific real-world entity; the protocol itself supplies no mechanism to establish that correspondence. A web-of-trust binding approach requires an existing social graph dense enough to avoid th |
| `AMPARORE-ARXIV-26` | D | Range-Based Set Reconciliation via Range-Summarizable Order-Statistics Stores | RBSR over an RSOS backend requires the reconciling parties' data to be embedded in one totally ordered universe with a defined strict order relation, since every operation (Aggregate, Rank, Select, Enumerate) is defined over contiguous ranges of that order; Negentropy's concrete instantiation orders records lexicographically by (timestamp, 256-bit identifier), so any component storing data reconci |
| `ANAOBI-WWW-23` | K | Will Admins Cope? Decentralized Moderation in the Fediverse | WatchGen's global (central-broker) variant requires a component that can aggregate per-instance moderation-relevant metadata (post counts, hate-word counts, prior policy actions, user counts) across all participating instances into one training pool; this conflicts with a design in which no party holds a global view of instance activity. The local (decentralized) variant instead requires each inst |
| `ANDERSON-CCGRID-06` | C | The Computational and Storage Potential of Volunteer Computing | Requires that whatever component consumes this capacity estimate tolerate the redundancy overhead the paper describes as necessary for correctness: because "anonymously volunteered computers can't be trusted," the paper's own capacity model already discounts raw hardware capacity by a redundant-computing factor of slightly more than two, so any storage or computation layer built on this host popul |
| `ANDONI-ARXIV-26` | B | Efficient Algorithms for Adversarially Robust Approximate Nearest Neighbor Search | Every construction requires the algorithm's setup-phase and per-query internal randomness to be genuinely private from the adversary (Definition 3.1 states the adversary "may know the code for A but not the specific instance of R_setup"); a real deployment must therefore supply a source of randomness the querying party (or anyone observing the querying party's queries and answers) cannot predict o |
| `ANGEL-SP-18` | G | PIR with Compressed Queries and Amortized Query Processing | The client must already know, before issuing a query, which numeric index or indices in the database it wants; the paper states three alternative external mechanisms an application must supply to produce that index (a Bloom filter shared out of band, as in the Pung system; a PIR-by-keyword scheme; or the client independently simulating the server's Cuckoo-hashing placement given a shared pseudoran |
| `ARREGUI-GARCIA-ARXIV-26` | K | On the Effects of Decentralized Moderation on Network Robustness and Information Diffusion | The stationarity finding requires daily-resolution, longitudinal domain-block records per instance-pair over a period long enough to aggregate to multi-week windows (approximately 20 days for KL divergence to approach zero); a system that logs only current block state, without a dated history of block issuance, cannot reproduce this measurement. The information-diffusion simulation requires a posi |
| `ASTHANA-ICTIR-11` | B | On the Feasibility of Unstructured Peer-to-Peer Information Retrieval | Achieving the stated accuracy figures requires the query rate assumption (1,000 queries/second across 1,000,000 nodes) to hold; the per-query cost and per-peer bandwidth figures scale directly with both the query rate and the node-query fan-out z, so a system with a different member count or query rate needs the underlying equations (Equations 8-13) re-evaluated with its own n, m, and target accur |
| `ASTHANA-PHD-14` | B | PAC'nPost: A Peer-to-Peer Micro-blogging Social Network | PAC'nPost requires an underlying peer-sampling/membership service that supplies each node with a partial, near-uniformly-random view of the network and periodically refreshes that view by exchanging entries with other nodes; the thesis names Cyclon, the overlay management system of Jelasity et al., and Brahms as examples that could supply this, but does not itself implement or evaluate any of them |
| `ATENIESE-CCS-07` | C | Provable Data Possession at Untrusted Stores | Requires the client to retain the full file (or reconstruct it) at Setup time in order to compute one tag per block before deletion, and requires the client to retain a small constant amount of secret key material (about 3 KB in the paper's 4 GB worked example) for the life of the stored file; losing that key material makes the client unable to issue or verify any future challenge. Requires the st |
| `ATTIYA-PODC-16` | D | Specification and Complexity of Collaborative Text Editing | Any protocol this lower bound governs must supply causal broadcast at minimum — the paper's model requires that if send(m) happens-before send(m′), no replica receives m′ before m — and the paper's results are proved to hold even under the strictly stronger causal atomic broadcast (adding no-duplicate-delivery and a single consistent global delivery order across all replicas), so a system that onl |
| `AUERBACH-CRYPTO-25` | H | Continuous Group-Key Agreement: Concurrent Updates Without Pruning | The security proof for MLS-Cutoff (Theorem 5.1) is conditioned on the underlying public-key encryption scheme being IND-CCA2 secure, the signature scheme being strongly existentially unforgeable under chosen-message attack (SEUF-CMA secure), and the protocol's hash and key-derivation functions and message authentication code being modeled as random oracles / ideal primitives, matching the assumpti |
| `AUVOLAT-SRDS-19` | D | Merkle Search Trees: Efficient State-Based CRDTs in Open Networks | Every replica must use the same hash function and the same base-B assignment rule to place items into tree layers, and that hash function must be collision-resistant (the paper states the probability of two colliding strings is assumed negligible) and must project uniformly across values of size a power of B, since the layer-depth and balance guarantees derive directly from that uniformity; the pa |
| `AZARI-ACNS-25` | H | Tighter Provable Security for TreeKEM | The bound requires the deployed CGKA construction to be exactly TreeKEM instantiated with DHIES for its public-key encryption component; the proof structure (splitting the security loss into an EAV-security term for the private-key encryption sub-component and a Diffie-Hellman-hardness term) does not apply as stated to a different combination of primitives. It requires whatever key-derivation func |
| `BACK-HASHCASH-02` | I | Hashcash - A Denial of Service Counter-Measure | A deployment needs a server-side store, keyed by spent token, that persists at least as long as the timestamp-derived expiry window it uses to reject double-spent tokens; without an accurate, loosely-synchronized clock across server and client, the timestamp-based expiry either rejects valid recent tokens or fails to bound database growth. The service-name string bound into each token must be chos |
| `BALBAS-ASIACRYPT-23` | H | WhatsUpp with Sender Keys? Analysis, Improvements and Security Proofs | Requires authenticated, secure two-party channels between every pair of group members (instantiated with the Double Ratchet in deployment) — every member must independently maintain n−1 such channels. Requires a central server (or equivalent delivery service) that provides total ordering of control messages to all parties; total ordering is not required for application messages. The Sender Keys+ u |
| `BALBAS-USENIXSEC-23` | H | Cryptographic Administration for Secure Group Messaging | Requires an incorruptible PKI: every party, and separately every administrator, must have a registered, authenticated public key that other parties can retrieve (getSpk/registerKeys), and both IAS and DGS assume this PKI functions correctly throughout — the paper does not model PKI corruption. Requires an underlying CGKA (e.g., MLS/TreeKEM) to already supply forward secrecy and post-compromise sec |
| `BALDUF-IMC-23` | A | The Cloud Strikes Back: Investigating the Decentralization of IPFS | This is a measurement paper and defines no mechanism for another system component to depend on. Its results constrain claims a synthesis can make about deployed content-addressed storage: any claim that a Kademlia-DHT-based content-location layer is decentralized in practice must state which channel (topology, traffic volume, content-provider population, or entry points) the claim covers, because  |
| `BALDUF-IMC-24` | J+K | Looking AT the Blue Skies of Bluesky | A component-diversity claim about Bluesky (or an architecture like it) requires specifying which component is meant, since this paper's own results diverge sharply by component: identity-handle hosting is stated as 98.9% concentrated in the platform operator's own free domain, Feed-Generator hosting-as-a-service is stated as 85.86% concentrated in one third-party platform (Skyfeed), while domain r |
| `BARNES-RFC-23` | H | The Messaging Layer Security (MLS) Protocol | The Delivery Service must deliver a Commit and its accompanying Welcome message in a way that lets every recipient compute the same next epoch state; the specification does not itself provide agreement on which Commit is canonical when two members generate Commits concurrently from the same epoch. The document requires (MUST) that applications establish some way to resolve conflicting Commit messa |
| `BARTUSEK-EPRINT-26` | H | Fair-Weather No More: Guaranteed Efficiency in Secure Group Messaging | The construction requires a common reference string (crs) sampled once at setup, shared by all group members. It requires each party to store the full current group roster and every other current member's public key, so any component using this CGKA must supply and keep that per-member public-key list current at every party. It requires message delivery to be honest, in the sense that the adversar |
| `BENET-ARXIV-14` | C | IPFS - Content Addressed, Versioned, P2P File System | Requires every participating node to provide local storage (disk or, for non-persistent caches, RAM) in which retrieved blocks are held, because "all blocks available in IPFS are in some node's local storage" and there is no other persistence layer; a node with no local storage cannot serve content it does not currently have live in a network buffer. Requires an explicit pinning operation for any  |
| `BENET-FILECOIN-17` | C | Filecoin: A Decentralized Storage Network | Filecoin's PoRep/PoSt layer requires an underlying content-addressed storage and transfer layer beneath it: the paper states Filecoin "works as an incentive layer on top of IPFS," and Get/Put move the actual bytes outside the proof protocol. The consensus mechanism (Expected Consensus) requires a source of public, unpredictable per-epoch randomness drawn from the blockchain itself to issue non-int |
| `BENHAMOUDA-ASIACRYPT-23` | I | Anonymous Counting Tokens | Every construction requires a client registration step (`ClientRegister`) before any token issuance: the client generates a long-term key pair with the issuer and this registered key is the input to the rate-limiting pseudorandom function that produces the throttling tag; a system without a prior client-registration phase cannot supply this. The randomness `r` used to combine a message with the ps |
| `BERNSTEIN-EUROCRYPT-15` | E | SPHINCS: Practical Stateless Hash-Based Signatures | A verifier must have access to the same hash-function definitions (F, H, the message-hash function, and the PRG) as the signer, since signature verification recomputes hash-tree paths using these exact functions; substituting a different hash function without renegotiating the scheme breaks compatibility, not just security. The scheme requires no persistent mutable state at the signer between sign |
| `BEURDOUCHE-RFC-25` | H | The Messaging Layer Security (MLS) Architecture | The document requires an Authentication Service the deploying application trusts for identity-to-key binding, whatever its internal structure; MLS's confidentiality guarantee for the group key is conditioned on correct AS behavior, not on correct DS behavior. It requires a Delivery Service that can store and serve one-time-use KeyPackages, route messages to specific clients or to a whole group, an |
| `BHAGWAN-IPTPS-03` | C | Understanding Availability | A replication-strategy calculation that consumes this paper's availability numbers must use a time-window-matched availability figure, not a single global mean, because the measured distribution's shape changes with the window length over which availability is computed (10 hours vs. 4 days vs. 7 days produce different curves from the same host population). A system that identifies hosts by IP addr |
| `BHAGWAN-NSDI-04` | C | Total Recall: System Support for Automated Availability Management | The redundancy calculation (Equations 1-5) requires a measured, current, empirical distribution of host availability specific to the deployment population; the paper explicitly rejects using a single assumed mean-time-to-failure figure because host populations are heterogeneous and must be measured, not assumed. Lazy repair requires the storage layer to maintain explicit per-file metadata recordin |
| `BHARGAVAN-HAL-18` | H | TreeKEM: Asynchronous Decentralized Key Management for Large Dynamic Groups | The construction requires that when a new device joins a group, some existing member deliver it the current group-messaging key derived from hashing the prior group key with the new root key, because a new device cannot derive this by itself from the tree alone. It requires, for concurrent-operation handling, a merge policy that orders operation types relative to each other — the paper's own examp |
| `BIENSTOCK-TCC-20` | H | On the Price of Concurrency in Group Ratcheting Protocols | Requires a synchronous, authenticated broadcast channel on which every member receives every sender's ciphertext in the same round — the model gives no mechanism for asynchronous or partial delivery. Requires an honest-but-curious external mechanism that clocks rounds consistently for all members; the authors state this clock synchronization can be coarse without undermining the result. The upper- |
| `BIENSTOCK-TCC-22` | H | On the Worst-Case Inefficiency of CGKA | Any CGKA construction that wants to escape this paper's Ω(n) worst-case bound must not treat its underlying public-key encryption as a sealed black box — it must exploit PKE's internal algebraic structure directly, or substitute a non-black-box primitive, because the impossibility result covers every protocol that uses PKE only through its stated encrypt/decrypt interface. A system deploying a bla |
| `BIRYUKOV-EUROSP-16` | I | Argon2: New Generation of Memory-Hard Functions for Password Hashing and Other Application | A system deploying Argon2 must choose between Argon2d and Argon2i (or a hybrid) based on whether the execution environment can leak timing information to an adversary: the document's own selection procedure directs the caller to pick Argon2i whenever side-channel observation is a viable threat, and either variant otherwise, meaning the calling system must characterize its own co-tenancy and physic |
| `BIRYUKOV-FC-15` | I | Proof-of-Work as Anonymous Micropayment: Rewarding a Tor Relay | Requires the relay to hold or obtain a crypto-currency mining-pool account and requires that pool to accept proof-of-work shares of the difficulty the relay assigns to clients; the relay's earned coins never touch the client. Requires a partially blind signature scheme (the paper specifies a WI-Schnorr-based partially blind signature construction) under which the timestamp attribute is visible to  |
| `BIRYUKOV-SP-13` | J | Trawling for Tor Hidden Services: Detection, Measurement, Deanonymization | Any rendezvous/directory design that assigns responsibility for storing a lookup record deterministically from a public identifier (a fingerprint ring keyed by a hash of the identifier, here) gives an adversary who can compute future identifiers the ability to precompute keys that land adjacent to that identifier and, by running relays with those keys, capture the storage role before the legitimat |
| `BLAKE-HOTOS-03` | C | High Availability, Scalable Storage, Dynamic Peer Networks: Pick Two | Any redundancy scheme relying on this paper's bandwidth bound requires an accurate, population-specific measurement of `N` (host count), `a` (fractional availability), and `T` (average membership lifetime) — the paper explicitly computes its own figures from an original Gnutella crawl rather than assuming a generic churn rate, and states the bound is only as good as those inputs. A system that wan |
| `BOLDYREVA-PKC-03` | E | Threshold Signatures, Multisignatures and Blind Signatures Based on the Gap-Diffie-Hellman | The threshold scheme TGS requires an underlying communication substrate providing both a broadcast channel and secure point-to-point channels among all n participants; the paper states this explicitly as its assumed communication model, and the distributed key generation protocol (DKG) it reuses from Gennaro-Jarecki-Krawczyk-Rabin depends on this channel structure for its own security guarantees.  |
| `BONEH-ASIACRYPT-01` | E | Short Signatures from the Weil Pairing | Requires a source of a Gap Diffie-Hellman group: a group with an efficient algorithm for deciding Diffie-Hellman tuples but no known efficient algorithm for computing them, which in this paper is supplied only by the Weil-pairing-equipped supersingular elliptic curves described, not by an arbitrary discrete-log group. Requires a hash function that can be treated as a random oracle for the MapToGro |
| `BONEH-ASIACRYPT-16` | I | Balloon Hashing: A Memory-Hard Function Providing Provable Protection Against Sequential A | The security proof requires the compression function `H` to behave as a random oracle; a real deployment substitutes a concrete hash function and inherits whatever gap exists between that function and true random-oracle behavior. The proof further requires `H`'s internal state size to be at least as large as its output size — the paper states this specifically to avoid an unspecified pitfall detai |
| `BONEH-ASIACRYPT-18` | E | Compact Multi-Signatures for Smaller Blockchains | Every signer must have generated its key pair before any aggregation or signing occurs, and the aggregation step (or, for PoP variants, the possession-proof verification) must run over the full identical set of participating public keys that later signers use, since the per-key aggregation exponent is itself a hash of that whole set. A pairing-friendly elliptic curve (an asymmetric bilinear group  |
| `BONEH-CRYPTO-03` | E | Aggregate and Verifiably Encrypted Signatures from Bilinear Maps | Every signature that will be aggregated must be over a message value produced by feeding the message through the shared full-domain hash function H into G1; a signer or protocol that hashes messages differently cannot participate in the same aggregate. The aggregator needs no private key and can be an untrusted third party, but it must have received every individual signature to be included and mu |
| `BONEH-CRYPTO-04` | E | Short Group Signatures | A pairing-friendly bilinear group pair (G1, G2) with an efficient isomorphism from G2 to G1 must be available, and the group manager's tracing key (xi_1, xi_2) must be generated and held separately from the SDH secret gamma used at key-issuing time — gamma must not survive key generation in the hands of any party, including the group manager, or every user's untraceability guarantee is void. The b |
| `BONEH-CRYPTO-18` | I | Verifiable Delay Functions | A VDF-based randomness beacon requires the network to post public parameters (from Setup) before any challenge is issued, and requires whoever evaluates the VDF to have no way to learn the challenge input earlier than every other party, since the entire security argument rests on the evaluator needing t sequential steps starting from when the input becomes known. The trusted-setup variants (the IV |
| `BONO-ICWSM-26` | K | Self-moderation in the Decentralized Era: Decoding Blocking Behavior on Bluesky | The mechanism requires a data source that streams every block action publicly and in real time, the way Bluesky's Firehose does; the authors state explicitly that this data is typically inaccessible on other platforms, including on another decentralized platform, Mastodon, where blocks are not similarly exposed. The feature set requires access to each user's full action history (likes, posts, repo |
| `BONO-WEBSCI-24` | K | An Exploration of Decentralized Moderation on Mastodon | The measurement method requires each instance to expose its blocklist and ban motivations through the public domain_blocks API endpoint; the authors state some instances explicitly obfuscate the domain of a moderated instance, which is a participant-controlled opt-out from full transparency that the measurement can only partially work around. The similarity and clustering analysis requires the sam |
| `BORMANN-SSR-25` | E | SoK: Anonymous Credentials for Digital Identity Wallets | BBS-based deployment requires all issuers, holders, and verifiers currently using ECDSA-based attribute credentials to change their software, since BBS is a different signature scheme entirely — this is stated explicitly as a migration cost distinguishing it from the general-purpose-ZKP approach. It requires either approval and deployment of a pairing-friendly elliptic curve across the ecosystem,  |
| `BOSHMAF-NDSS-15` | F | Íntegro: Leveraging Victim Prediction for Robust Fake Account Detection in OSNs | Íntegro requires a set of manually verified trusted accounts (seeds) distributed across the graph's detected communities; verification is performed by human analysts, and the paper states the number of seeds selectable is bounded by available manual-verification resources, with a stated minimum of one seed per detected community. It requires a community-detection step (the Louvain method, run befo |
| `BOSWELL-ARXIV-24` | L | Measuring NAT64 Usage in the Wild | Any peer-reachability design that assumes IPv6-only clients can always reach IPv4-only destinations, or vice versa, through NAT64 needs to account for measured NAT64 deployment being rare among the probes tested (255 of 6,154, about 4.1%) — most tested networks have no detectable NAT64 path at all, so a design cannot assume a translator will be present on an arbitrary IPv6-only client's path. Wher |
| `BRIGHTID-WP` | E | BrightID Whitepaper | An application that wants Sybil-resistant per-user verification must register a context (an identifier system plus a chosen verification method) with one or more BrightID nodes, and must independently decide which nodes' verification responses to trust — nothing in the base protocol enforces node honesty for a single-node context; the whitepaper states this is a centralization risk the application |
| `BUCHET-CCR-25` | L | An Analysis of QUIC Connection Migration in the Wild | A NAT-traversal or reachability design that depends on QUIC connection migration to survive an address change needs to account for measured migration support being far from universal even among servers that speak QUIC and pass a full TLS handshake: with SNI supplied (the condition closest to a normal client connecting to a named service), migration succeeded for 52% of IPv4 targets and 78% of IPv6 |
| `BURCKHARDT-POPL-14` | D | Replicated Data Types: Specification, Verification, Optimality | Every consistency-axiom proof in the paper's store-composition result (Definition 20, Proposition 19) requires each object's data-type implementation to be independently proved correct against its own specification under some visibility witness, and requires the transport layer to satisfy a stated transport-layer specification (T-Any or a specific variant) matching that witness; the paper states a |
| `CAI-CCS-14` | G | A Systematic Approach to Developing and Evaluating Website Fingerprinting Defenses | Tamaraw requires a proxy or client-side component capable of enforcing fixed-rate, fixed-size packet transmission independently in each direction, and requires that component to determine, out of band from the padded stream itself, when the real data transfer for a page has ended (the paper flags this as non-trivial and assumes an oracle for it in its own simulation rather than solving it). The sc |
| `CAMENISCH-TRUST-16` | E | Anonymous Attestation Using the Strong Diffie-Hellman Assumption Revisited | A verifier checking a non-revocation proof must supply a fixed, application-specific basename bsn for every verification context, since the platform's pseudonym is computed as a hash of that basename raised to the platform's secret key; a system that omits basenames (fully anonymous verification) is not supported by this scheme and must instead have platforms generate a single-use basename per sig |
| `CAO-NSDI-12` | F | Aiding the Detection of Fake Accounts in Large Scale Social Online Services | Requires the operator to hold the complete social graph as an undirected graph, and requires an initial manually verified set of non-Sybil trust seeds (drawn per detected community, not globally) before the mechanism can run at all. Requires the non-Sybil region of the graph to be well-connected, non-bipartite, and fast-mixing relative to the full graph including Sybils (mixing time strictly less  |
| `CASH-CCS-15` | G | Leakage-Abuse Attacks Against Searchable Encryption | A design using searchable encryption for network-hosted content must state, for its actual index construction, which of the four leakage profiles (L1-L4) it produces, since the attacks in this paper apply differently by profile: L1 requires the adversary already possess or estimate the true document-count-per-keyword to mount the count attack; L2 and L3 are vulnerable to known-document and chosen- |
| `CASTRO-OSDI-02` | A | Secure Routing for Structured Peer-to-Peer Overlay Networks | The certified-nodeId mechanism requires an external certification-authority infrastructure whose public keys are distributed out of band (the paper suggests bundling them with node software, as browsers bundle root certificates), and requires a certificate-revocation or reissuance path for any node whose IP address changes, since the paper binds nodeId certificates to IP addresses specifically to  |
| `CASTRO-SOSP-03` | K | SplitStream: High-Bandwidth Multicast in Cooperative Environments | SplitStream requires a structured peer-to-peer overlay with prefix-based routing (the paper uses Pastry) whose group-communication layer builds each multicast tree as the union of overlay routing paths from members to a group's rootId (the paper uses Scribe); the interior-node-disjoint property is obtained purely by choosing k groupIds that differ in their most significant digit, so it depends on  |
| `CHASE-CCS-19` | E | SEEMless: Secure End-to-End Encrypted Messaging with less</> Trust | A working SEEMless deployment needs at least one honest auditor examining every adjacent pair of epochs; the non-equivocation soundness guarantee is stated as conditional on that minimum, and auditing can be distributed arbitrarily across many independent parties without coordination since each adjacent-epoch check is independent. The system needs an external, out-of-band mechanism ensuring all cl |
| `CHASE-CRYPTO-20` | G | Private Set Intersection in the Internet Setting From Lightweight Oblivious PRF | Both parties must already know the OPRF sender/receiver assignment and must both compute over sets of comparable, explicitly stated size — the measured results assume equal-size sets (n1 = n2 = n) and do not characterize behavior for a small client set against a large server set. A deployment giving this protocol's asymmetric malicious-security guarantee needs a trust assignment decision made in a |
| `CHEN-TPDS-09` | B | Difficulty-Aware Hybrid Search in Peer-to-Peer Networks | The scheme requires a two-tier overlay in which a small subset of nodes (chosen by the system as long-uptime, server-like) forms a DHT and every other peer connects through an unstructured super peer to that DHT; a flat, single-tier unstructured network with no capacity-heterogeneous node subset has no natural candidate for the structured-super-peer role the paper's design depends on. Computing th |
| `CHENG-ACSAC-20` | G | Talek: Private Group Messaging with Hidden Access Patterns | Requires an anytrust deployment: at least one of the l participating servers, chosen from among mutually distrusting operators, must actually be honest, though the protocol does not require knowing in advance which one. Requires server storage capacity that scales with the number of clients, since every server holds a full copy of the shared message database. Requires that communicating clients al |
| `CHEVALIER-CCS-24` | H | Quarantined-TreeKEM: A Continuous Group Key Agreement for MLS, Secure in Presence of Inact | QTK requires an underlying TreeKEM-based CGKA with a Ratchet Tree (a full binary tree with users at leaves) and a Propose & Commit round structure, and the authors state it stays compatible with the family of TreeKEM-derived CGKAs in the literature (CoCoA, DeCAF, RTreeKEM, Tainted TreeKEM). It requires an Authentication Service assumed secure enough that member corruption never leaks the corrupted |
| `CHOR-JACM-98` | G | Private Information Retrieval | Every scheme (Sections 3-6) requires at least two physically or organizationally separate copies of the database that do not communicate their received queries to each other; the 1-privacy guarantee is voided the moment any two of the k databases pool what they each received, since the paper states directly that colluding pairs can extract information about, and in some schemes fully recover, the  |
| `CHOU-USENIXSEC-25` | H | Bots can Snoop: Uncovering and Mitigating Privacy Risks of Bots in Group Chats | SnoopGuard requires an underlying tree-based CGKA (the paper builds specifically on TreeKEM) that already delivers FS and PCS to the human-user subtree; it does not supply those properties itself, it inherits them. It requires every user client to run a trigger function locally to classify incoming plaintext messages for chatbot relevance before encryption, meaning message content must be visible  |
| `CHROMIUM-BLINK-SRC` | L | rtc_peer_connection.cc: kMaxPeerConnections limit enforced by RTCPeerConnection constructi | A browser-tab network participant that opens one `RTCPeerConnection` per remote peer is capped at 500 simultaneous peer connections in that tab, regardless of the application's own connection-management policy. Any overlay or mesh design that maps peers to `RTCPeerConnection` objects one-to-one must keep the per-tab active-peer count under 500 or handle the thrown exception when a new connection a |
| `CHU-JSAC-02` | K | A Case for End System Multicast | Narada requires each member to maintain a complete list of all other group members, updated through periodic refresh messages propagated along the mesh; the authors state this scales poorly and is a deliberate tradeoff for two properties: independence from any single non-failing external node (an out-of-band bootstrap mechanism is used only to let new members join, and its failure does not disrupt |
| `CHUN-NSDI-06` | C | Efficient Replica Maintenance for Distributed Storage Systems | Carbonite requires each node to maintain per-object soft state recording the current set of nodes (available or recently failed) holding each replica it is responsible for, and requires this state to fail over to a successor node when the responsible node itself fails — the paper states this monitoring state is "soft" and can be transferred relatively transparently, in contrast to the hard state o |
| `CHYSTIAKOV-ARXIV-25` | D | Cartesian Merkle Tree | A verifier must know the current Merkle root and trust the deterministic priority function `PH` used to build the tree; if a party inserting elements could choose `PH` outputs adversarially, the history-independence property (same key set implies same tree shape regardless of insertion order) would not hold, since the whole point of deriving `p` from `k` deterministically is to remove insertion-or |
| `COHEN-IPTPS-03` | I | Incentives Build Robustness in BitTorrent | BitTorrent's choking mechanism requires a bidirectional TCP connection between any two peers exchanging pieces, since choking blocks only the upload direction while leaving the same connection able to resume downloading without renegotiation. It requires the tracker to supply peer lists chosen at random rather than through a structured peer-selection rule, since the paper attributes BitTorrent's c |
| `COHEN-SIGCOMM-02` | B | Replication Strategies in Unstructured Peer-to-Peer Networks | The distributed replication algorithms require that copy deletion be independent of which item is stored and of that item's access pattern -- an eviction policy that instead prunes least-recently-used or least-frequently-used copies breaks the fixed point the algorithms converge to, because it makes deletion rate depend on query rate. Path replication and sibling-number memory both require the req |
| `COHNGORDON-CCS-18` | H | On Ends-to-Ends Encryption: Asynchronous Group Messaging with Strong Security Guarantees | ART requires a one-round authenticated key exchange protocol (used by the group creator to derive each future member's leaf secret from that member's published prekeys) but states no specific structural requirement on that sub-protocol beyond it being a strong one-round Authenticated Key Exchange (AKE). It requires every group member to maintain a persistent local view of the DH tree's public-key  |
| `COHNGORDON-CSF-16` | H | On Post-Compromise Security | The paper's core PCS-achieving construction under total compromise requires persistent, synchronized secret state shared between the two communicating parties across sessions ("intermediate secrets"), so any system adopting it must provide durable per-peer storage that survives between sessions, not just within one session; a fully stateless implementation cannot satisfy PCS under total compromise |
| `COHNGORDON-EUROSP-17` | H | A Formal Security Analysis of the Signal Messaging Protocol | Signal's asynchronous operation requires a prekey distribution server that stores and relays prekeys and messages but performs no cryptographic computation of its own — the paper's proof depends on this server being untrusted for message secrecy (any active-adversary manipulation of prekey delivery is within the modeled threat model) but implicitly relies elsewhere on that server, or an out-of-ban |
| `CORRIGANGIBBS-CCS-10` | G | Dissent: Accountable Anonymous Group Messaging | Every member must hold a public encryption key and a nonrepudiable signing key known to every other member before a run begins (Section 2.4); the paper's core analysis assumes this key distribution is already solved and does not itself provide key management. All N members participating in a run must remain connected and continue sending correctly signed messages until the run completes; the paper |
| `CORRIGANGIBBS-SP-15` | G | Riposte: An Anonymous Messaging System Handling Millions of Users | The security property (no server learns which client wrote which row) holds only under the stated non-collusion assumption: no two of the three servers collude in the three-server variant, or no more than s-1 of s servers collude in the general variant; any system composing Riposte as a subcomponent must independently ensure operationally that the servers it selects do not collude, since Riposte i |
| `CORTESGOICOECHEA-ARXIV-24` | A | Scalability Limitations of Kademlia DHTs when Enabling Data Availability Sampling in Ether | - Requires a fixed per-operation deadline (the paper's is the Ethereum 12-second slot time) against which any DHT-based content-location mechanism's seeding and lookup latency must be measured; a mechanism whose seeding time scales with the number of items to publish from a single node, as Kademlia's provide operation does here, cannot meet a fixed per-round deadline once item count crosses a thre |
| `COSTA-DAIS-23` | J | Studying the Workload of a Fully Decentralized Web3 System: IPFS | None as a positive mechanism — this paper is a measurement study of a system it does not modify. What it establishes as required background for interpreting these figures: the measurements assume the IPFS DHT correctly resolves FindProviders queries to the peers currently advertising a cId, and that gateway logs faithfully record every client-facing HTTP request; a provider count or replication fi |
| `COX-ECIR-10` | B | Improving Query Correctness Using Centralized Probably Approximately Correct (PAC) Search | The scheme requires a centralized coordination node that receives every query, performs the pseudo-random seed-based computer selection, retains per-query state (the identifiers of the retained computer subset from the previous iteration, called node caching, distinct from caching retrieved documents), and forwards the query to the resulting computer set; this node must be reachable by every query |
| `COX-ICTIR-09` | B | Probably Approximately Correct Search | Each participating computer must be able to draw an unbiased random sample of documents from the full collection during acquisition; the paper states this capability is "difficult, but certainly possible" and does not itself provide a mechanism, only assumes one for the decentralized case (the centralized case can substitute a single centralized crawler with random, non-disjoint partitioning). The |
| `COX-SOSP-03` | I | Samsara: Honor Among Thieves in Peer-to-Peer Storage | Samsara requires that objects are replicated across multiple independent sites so that a probabilistic per-replica discard on a failed query does not destroy the only copy of an object; the punishment mechanism assumes replication is already the availability strategy, not an add-on. It requires periodic bidirectional querying between every pair of nodes in a storage relationship (the querying sche |
| `CRESPO-AP2PC-04` | B | Semantic Overlay Networks for P2P Systems | Every participating node must run the same classification hierarchy and the same (or a compatible) document classifier, because SON membership is determined by which hierarchy category a document's automatic classification falls into; a node using a different hierarchy would join incompatible SON identifiers and queries would not reach it. Query issuers must classify each outgoing query into the s |
| `CRITES-CCS-25` | F | SyRA: Sybil-Resilient Anonymous Signatures with Applications to Decentralized Identity | SyRA requires an external mechanism supplying the personhood relation R_s — a way to verify that a claimed real-world identity string s is genuinely tied to one physical person and not previously used. The paper explicitly does not build this: it lists a signed government certificate (e.g., an X.509 attribute certificate over a national ID), a biometric reading (citing Worldcoin), or an OAuth/Open |
| `CZYZ-SIGCOMM-14` | L | Measuring IPv6 Adoption | A design assuming IPv6-only or IPv6-preferred direct reachability between arbitrary peers, evaluated against this paper's 2014 endpoint, must assume only 3.2-3.5% of popular web servers and roughly 2.5% of client vantage points had working IPv6 at that time — a design that requires IPv6 transport for a majority of participants had no basis in this dataset's period and needs a more recent measureme |
| `DANEZIS-NDSS-09` | F | SybilInfer: Detecting Sybil Nodes using Social Networks | SybilInfer requires each verifying node to know at least one honest node a priori (used as the sampling seed) to break the symmetry between the honest and dishonest regions; without a known-honest seed, an attacker mirroring the honest social structure is indistinguishable from the real one. It requires the social graph to be fast-mixing, a property the paper attributes to real social networks by  |
| `DANEZIS-SP-03` | G | Mixminion: Design of a Type III Anonymous Remailer Protocol | The client must obtain relay public keys and per-relay performance/reliability statistics from a directory-server infrastructure before constructing any path; the design assumes a threshold of directory servers remains honest and requires clients to hold the entire directory rather than a partial view, to prevent an adversary from exploiting differences in client directory knowledge. Anonymity aga |
| `DANEZIS-WALRUS-25` | C | Walrus: An Efficient Decentralized Storage Network | RedStuff requires an external, black-box blockchain substrate that accepts a concurrent transaction set and outputs a single total order of state updates without indefinite censorship; the paper states this abstraction, not a specific consensus algorithm, is what RedStuff needs, and any state-machine-replication protocol meeting it would suffice (the deployed system uses Sui). The 2D encoding's re |
| `DAVIDSON-POPETS-18` | I | Privacy Pass: Bypassing Internet Challenges Anonymously | The protocol requires the server to run and maintain a signing key, publish a small, low-churn list of currently accepted verification commitments (2-3 keys recommended) so clients whose tokens were signed under a recently-rotated-out key can still redeem before those tokens expire, and requires some out-of-band or globally-consistent channel for distributing key-rotation updates; the paper sugges |
| `DAVIDSON-POPETS-23` | G | FrodoPIR: Simple, Scalable, Single-Server Private Information Retrieval | The client must know, before the online phase, the specific numeric index it wants to query; the paper states elsewhere (Section 7, referring to Kogan and Corrigan-Gibbs) that mapping a real-world key (such as a URL hash) to that index requires a separate mechanism the client downloads out of band, distinct from the FrodoPIR protocol proper. The scheme requires an offline phase before any query: t |
| `DHAMDHERE-IMC-12` | L | Measuring the Deployment of IPv6: Topology, Routing and Performance | A design that assumes an IPv4 and an IPv6 path between the same pair of ASes traverse the same intermediate networks cannot rely on that assumption for this paper's measurement period: only 40-50% of such paths were identical, so protocol-specific routing decisions (congestion control tuned to one protocol's path characteristics, or a latency budget derived from one protocol) cannot be transferred |
| `DICURSI-BIGDATA-24` | J | A Herd of Young Mastodonts: the User-Centered Footprints of Newcomers After Twitter Acquis | None as a positive system mechanism — this is a measurement and social-network-structure study, not a protocol proposal. The methodological requirement worth noting for anyone using this paper's figures: the ego-network circle-size results are drawn only from the post-acquisition-only user subgroup (1,534 of 1,999 collected users) after the 6-month-duration and 2-contact-minimum filters, so the re |
| `DIMAKIS-TIT-10` | C | Network Coding for Distributed Storage Systems | A newcomer generating a replacement fragment must be able to connect simultaneously to k (RC) or n−1 (OMMDS) other fragment-holders and receive coded data from each; this requires those holders to be locatable and reachable at repair time, which the paper does not itself provide (it assumes an underlying distributed storage substrate such as OceanStore, Total Recall, or DHash++ supplies node disco |
| `DINGLEDINE-USENIXSEC-04` | G | Tor: The Second-Generation Onion Router | Every relay on a circuit must correctly maintain per-circuit packaging and delivery windows and respond to `sendme` cells; a relay that does not implement this specific end-to-end congestion-control protocol breaks the flow-control assumption the rest of the circuit relies on to avoid accidental denial-of-service against volunteer-operated relays. Clients need continuous access to a directory-serv |
| `DOAN-NETWORKING-20` | K | Measuring Decentralized Video Streaming: A Case Study of DTube | A gateway-based content-addressed retrieval design requires the gateway operator to be reachable at low network-path length from the client, or startup delay and RTT degrade severely for clients geographically far from that operator (measured here as a 4x startup-delay penalty and 7-8 additional IP hops when the only gateway sits on another continent from the client). A system that isolates its ob |
| `DOUCEUR-IPTPS-02` | F | The Sybil Attack | A system relying on this paper's direct-validation defense must issue resource-demanding challenges (communication, storage, or computation puzzles) to every presented identity concurrently, system-wide, per Lemma 2; a validator that admits identities as they arrive rather than in synchronized rounds gets no bound on Sybil identity count. A system relying on indirect validation (vouching) must set |
| `DUARTE-ABAKOS-20` | L | Beware: NAT Traversal is a Simple and Efficient Approach to Open Firewall Holes | A design that relies on stateful NAT and firewall behavior to keep an internal host's unadvertised services unreachable from the public Internet cannot assume that property continues to hold once any peer-to-peer NAT-traversal client runs on a host inside the same private network: the traversal mechanism opens a bidirectional hole indistinguishable, from the firewall's perspective, from a hole ope |
| `DUCAS-TCHES-18` | E | CRYSTALS-Dilithium: A Lattice-Based Digital Signature Scheme | Verifying a Dilithium signature requires the verifier to reconstruct the high-order bits of Az-ct using the signer-supplied hint bytes, so any transport or storage layer carrying a Dilithium signature must preserve those extra hint bytes alongside (z, c) — dropping them makes verification impossible, not merely less efficient. The scheme's hash function H, modeled as a random oracle mapping (messa |
| `DURUMERIC-IMC-15` | J | Neither Snow Nor Rain Nor MITM...: An Empirical Analysis of Email Delivery Security | A store-and-forward relay protocol that fails open to cleartext on any handshake error (STARTTLS's behavior, as measured here) requires every relay hop, not just the endpoints, to correctly negotiate encryption, because a single corrupting hop anywhere on the path suffices to force cleartext delivery for that message, and the sender has no protocol-level signal that this occurred. Certificate-base |
| `DWORK-CRYPTO-92` | I | Pricing via Processing or Combatting Junk Mail | The junk-mail application as specified requires a single pricing authority trusted by all participating users to select the pricing function and hash function and to set usage fees, and requires that authority (or its trusted agents) to be the sole holder of the shortcut secret; a design that wants shortcut-based bulk-sending without a component playing this trusted-authority role cannot use the s |
| `DZIEMBOWSKI-CRYPTO-15` | C | Proofs of Space | The construction requires a cryptographic hash function modeled as a random oracle, with output length L chosen large enough that no party (including any adversary) can find collisions within the total query budget of the deployment; in practice this needs instantiation with a concrete hash function such as SHA3, which the paper notes carries only heuristic (not reduction-based) security. The veri |
| `ELAHI-WPES-12` | J | Changing of the Guards: A Framework for Understanding and Improving Entry Guard Selection  | Any capacity-ordered relay-selection mechanism that keeps a client's first hop fixed for a multi-week tenure (Tor's guard-flag design, measured here) requires the tenure length itself to be treated as a tunable security parameter with a measured trade-off against performance, because this paper measures that increasing the number of guards a client rotates through over a fixed period directly incr |
| `ELLIS-SIGMOD-89` | D | Concurrency Control in Groupware Systems | Every operator pair (u, v) in the application's operator set O needs a hand-specified transformation function T_uv before dOPT can run over that data model; the paper gives no automatic or general method for deriving T from an operator definition, so a system adopting dOPT for a different data structure than a character string must independently design and prove correct a new transformation matrix |
| `ENGELBART-ANRW-26` | L | QUIC as Multiplexing Layer in WebRTC | A WebRTC peer wishing to use this multiplexing scheme needs both peers' QUIC stacks to support the packet-receive-timestamp extension (draft-ietf-quic-receive-ts) before a real-time congestion controller such as GCC can run at the QUIC layer, because that extension is what lets the sender compute one-way delay the way RTCP receiver reports do in today's separate-stack WebRTC. The scheme also needs |
| `EPPSTEIN-SIGCOMM-11` | D | What's the Difference? Efficient Set Reconciliation Without Prior Context | Both hosts must agree in advance on the two hash functions Hc (checksum hash) and Hk (cell-index hash) and on the hash_count parameter, since decoding correctness depends on identical elements hashing to identical cell indices at both ends. The application must supply keys as fixed-size identifiers that fit the idSum XOR field (32-bit keys in the evaluation); the paper does not address variable-le |
| `EVERSPAUGH-USENIXSEC-15` | I | The Pythia PRF Service | Pythia requires the client to already possess a per-client key selector w (an "ensemble" identifier) established through an out-of-band Init call, and requires that selector, together with a server-held table entry K[w], to be treated as long-term state the server must retain and be able to update; loss of this table by the client or corruption at the server changes which PRF outputs subsequently  |
| `FAGIN-PODS-01` | B | Optimal Aggregation Algorithms for Middleware | TA requires the existence of a component that can perform sorted access to each of the m ranked lists in parallel and, on demand, random access to any list to fetch a named object's score in that list. A distributed implementation therefore requires each participant holding a scored list to answer a random-access lookup for an arbitrary object identifier, not only serve entries in sort order — thi |
| `FELDMANN-CSUR-21` | A | Survey on Algorithms for Self-Stabilizing Overlay Networks | The model underlying every protocol in this survey assumes a static node set: the survey states node departures are only addressed as a separate, later-developed extension (Section 7.2), and that termination of the self-stabilization process may not be reached if the node set changes during convergence, citing a proof of this in the surveyed literature. Every protocol in this survey's basic model  |
| `FISCH-EUROCRYPT-19` | C | Tight Proofs of Space and Replication | The Stacked-DRG PoS requires a cryptographic hash function (used as the graph-labeling function) and a vector-commitment scheme such as a Merkle tree, with the verifier retaining only the O(polylog(N))-size commitment root, not the data itself. For a PoRep built on this PoS to guarantee ε-rational replication (the property that storing k independently-generated replicas is the storage-minimizing s |
| `FORD-EUROSPW-17` | E | Proof-of-Personhood: Redemocratizing Permissionless Cryptocurrencies | Running one pseudonym party requires a set of volunteer organizers, each operating an independent server (conode) that together form a collective authority, and requires at least one organizer and at least one conode among them to be genuinely honest and non-colluding for the anytrust security assumption to hold; the specific honest party need not be identified in advance, but its existence is a p |
| `FORD-USENIX-05` | L | Peer-to-Peer Communication Across Network Address Translators | Hole punching requires a rendezvous server reachable by both peers before the peers can establish a direct session, and that server's role ends once the hole is opened — it never carries session traffic, unlike the relay-server alternative the paper contrasts it with (see DUARTE-ABAKOS-20's measured throughput collapse for a relay path under bandwidth constraint). The mechanism requires the NAT to |
| `FREEDMAN-NSDI-04` | A | Democratizing Content Publication with Coral | A DSHT client-facing application that needs the load-avoidance property depends on the underlying node-identifier space being large and effectively randomly distributed (Coral uses the SHA-1 hash of a node's IP address as its 160-bit identifier), because the tree-saturation-avoidance proof and the measured request-rate result at the closest node both assume the routing table's logarithmic-hop stru |
| `FREIRE-PKC-13` | H | Non-Interactive Key Exchange | Achieving the paper's default (plain, dishonest-key-registration) security model requires no assumption whatsoever about the Certificate Authority's registration checks — no proof of possession or knowledge of the private key is assumed — which the paper states is deliberately a stronger, more realistic assumption than models that assume such checks; a system layering identity binding or other gua |
| `FRIEDMAN-JEMS-01` | F | The Social Cost of Cheap Pseudonyms | The equilibrium results require a public history mechanism that every player can observe before acting: each pairing and each action taken must become common knowledge, either through direct interaction monitoring or explicit feedback collection: the model equates this to the environment's ability to publish a complete play history. The once-in-a-lifetime identifier mechanism requires a system-wid |
| `FURUTANI-TIFS-23` | F | Interpreting Graph-Based Sybil Detection Methods as Low-Pass Filtering | Every method covered by this framework, including SybilHeat, requires a set of pre-labeled benign nodes and, for methods other than SybilRank and CIA (which use only one label type each), a set of pre-labeled Sybil nodes as well; the paper's own experiments assume these labels arrive already correct except in the explicit noise-robustness tests, and do not address how a deployed system obtains the |
| `GAO-CNS-18` | F | SybilFuse: Combining Local Attributes with Global Structure to Perform Robust Sybil Detect | Requires a source of local, per-node (and ideally per-edge) attribute data sufficient to train a supervised classifier; when such attributes are unavailable, as in the paper's own Twitter evaluations, the method falls back to purely structural proxy features (accepted-request ratios, clustering coefficient) and a constant default for edge scores, which is a degraded configuration of the method rat |
| `GHOSH-HOTPETS-14` | I | A TorPath to TorCoin: Proof-of-Bandwidth Altcoins for Compensating Relays | Verifying that a claimed TorCoin corresponds to real bandwidth transfer requires a reader to hold the full public consensus log of shuffle matrices produced by assignment servers, since a Circuit Identifier is checked against that log and the four temporary public keys it names; a system without persistent access to that log cannot independently audit a coin. Assignment-server honesty is assumed o |
| `GILBOA-EUROCRYPT-14` | G | Distributed Point Functions and Their Applications | Two servers that do not collude with each other; the secrecy proof (Theorem 2, Definition 2) is stated for exactly two non-colluding servers holding an identical copy of the database, and the security argument fails if the same party controls both servers or observes both queries. A one-way function must exist in the deployment's cryptographic setting; if it is only a standard (not exponentially h |
| `GOEL-CORNELLTR-03` | G | Herbivore: A Scalable and Efficient Protocol for Anonymous Communication | Herbivore requires a distributed hash table (the paper uses Chord) to map clique keys to clique locations and to maintain predecessor/successor links between cliques during splits; the anonymity of the clique-assignment step assumes this lookup layer is not itself subverted, which the paper explicitly declines to analyze. It requires every pair of nodes within a clique to have securely exchanged a |
| `GOLD-ARXIV-23` | K | G-Rank: Unsupervised Continuous Learn-to-Rank for Edge Devices in a P2P Network | Peer discovery in G-Rank is not supplied by any separate mechanism: a peer becomes aware of another peer's existence only by receiving a clicklog gossip message naming that peer's node ID, so the ranking mechanism and the topology-discovery mechanism share one message type and G-Rank cannot be deployed without some initial bootstrap peer supplying a first clicklog sample to a joining node. The pul |
| `GOLDREICH-JACM-96` | G | Software Protection and Simulation on Oblivious RAMs | A physically shielded central processing unit is required to hold the k-bit internal secret state (the pseudorandom function seed and working registers); the security proof assumes this internal region cannot be observed or tampered with by the adversary, only the external memory access pattern is exposed. The external memory store must let the CPU perform arbitrary reads and rewrites of individua |
| `GOLLE-EC-01` | I | Incentives for Sharing in Peer-to-Peer Networks | The micro-payment and point-based mechanisms require a server, or an equivalent trusted component, that observes every download and upload transaction and can attribute each to a specific agent identity; the paper states the server tracks per-agent download and upload counts directly because it processes every download request. The mechanisms require agents to hold a persistent identity across tim |
| `GOMES-ARXIV-25` | D | Rateless Bloom Filters: Set Reconciliation for Divergent Replicas with Variable-Sized Elem | Both replicas must be able to compute the same hash function H and interpret slice index i identically, since correctness of the RBF partitioning depends on both replicas placing the same element into the same bit position for a given slice index. The final reconciliation phase requires fixed-size hashes of the variable-sized elements (not the raw elements) to be reconciled via IBLT, and after dec |
| `GONG-SP-22` | G | Surakav: Generating Realistic Traces for a Strong Website Fingerprinting Defense | Both the client and the entry-node proxy must run the regulator R and hold the same trained generator model; the regulator's Burst Adjustment and Random Response mechanisms require the proxy to receive a signal from the client (a message packet attached to each burst instructing how much data to respond with) so the two sides' bursts stay synchronized to the same reference trace. The threat model  |
| `GONG-TIFS-14` | F | SybilBelief: A Semi-Supervised Learning Approach for Structure-Based Sybil Detection | SybilBelief requires a social graph in which the homophily assumption approximately holds (linked nodes tend to share a label); the paper states this needs to be arranged elsewhere in the system, either by using an interaction-derived or tie-strength-derived graph rather than a raw friend graph, or by a preprocessing step elsewhere that detects and removes compromised benign nodes ("front peers")  |
| `GONG-USENIXSEC-20` | G | Zero-delay Lightweight Defenses against Website Fingerprinting | Both defenses require a cooperating relay to run the defense logic jointly with the client; the paper specifies this must be the middle node of a three-hop Tor circuit, not the entry or exit node, because the entry node is the position the adversary is assumed to occupy and the exit node and web server must see undefended traffic (the middle node strips dummy packets before forwarding onward). The |
| `GOODRICH-ALLERTON-11` | D | Invertible Bloom Lookup Tables | Both the structure's designer and any party issuing lookups must fix the threshold t (and correspondingly m, sized above (c_k + epsilon) * t) in advance, since the probabilistic guarantees for both GET and LIST_ENTRIES are conditioned on the actual number of stored pairs n staying at or below t at the moment of the operation — the paper is explicit that INSERT and DELETE always succeed and never e |
| `GREENE-EPRINT-25` | H | Making Post Quantum Key Exchange Efficient: An Implementation with the MLS Protocol | Requires maintaining two parallel MLS group sessions with identical membership at all times; any membership-management component must apply every Add/Remove to both sessions synchronously. Requires a mandatory Full Update (PQ session advance plus PSK injection into the traditional session) on every membership change; a system with frequent churn forces frequent PQ-cost operations regardless of the |
| `GREGORIADIS-ARXIV-25` | B | SwarmSearch: Decentralized Search Engine with Self-Funding Economy | Every retrieving node must locally hold a trained generative-retrieval model (De-DSI-style) whose output vocabulary spans candidate document identifiers, and a merge step requires numerically comparable per-agent beam scores (achieved here via per-agent softmax normalization before summing across agents) -- an agent returning scores on an incompatible scale corrupts the merge. Data Shapley valuati |
| `GRUBBS-SP-19` | G | Learning to Reconstruct: Statistical Learning Theory and Encrypted Database Attacks | A server or peer implementing an encrypted database that returns record identifiers per query — the minimal access-pattern leakage this paper attacks — must not treat that leakage as safe merely because the ciphertext and query content are hidden: the epsilon-AOR attack requires only the sequence of which stored items matched which range query, nothing about query content or plaintext values, and  |
| `GUMMADI-SIGCOMM-03` | A | The Impact of DHT Routing Geometry on Resilience and Proximity | A system that wants proximity route selection (PRS) or proximity neighbor selection (PNS) needs a routing geometry with route-selection or neighbor-selection flexibility greater than one (Table 1): tree and butterfly geometries structurally cannot supply either mechanism, hypercube can supply only PRS, and ring, XOR, and hybrid can supply both. Sequential-neighbor fallback routing, which the paper |
| `GUPTA-IPTPS-03` | A | Kelips: Building an Efficient and Stable P2P DHT through Increased Memory and Background O | Kelips requires every participating node to run continuous background gossip at a fixed, non-zero bandwidth cost regardless of query load, since heartbeat propagation (and therefore membership and filetuple correctness) depends on this steady background communication rather than on-demand maintenance triggered by lookups. It requires a cryptographic hash function (SHA-1 in the implementation) appl |
| `GUPTA-MOBICOM-24` | L | 3 W's of smartphone power consumption: Who, Where and How much is draining my battery? | A design that wants to cite this paper's cellular-versus-Wi-Fi or standby power figures for a continuous-participation cost estimate must use the axis-range figures above, not a fabricated per-bar milliwatt number, because the source figures in the retrieved text do not carry numeric data labels. Any claim about 5G standby cost relative to 4G is conditioned on both radios connecting to the same ba |
| `GUPTA-NSDI-04` | A | Efficient Routing for Peer-to-Peer Overlays | The scheme requires an underlying ring identifier structure with proven successor/predecessor maintenance (the paper explicitly reuses Chord's protocol and its correctness proof for this layer) — any substitute ring-maintenance layer would need an equivalent correctness guarantee for the one-hop and two-hop failure-recovery arguments to hold. Slice leaders and unit leaders bear materially higher b |
| `GUPTA-SIROCCO-20` | F | Resource Burning for Permissionless Systems | A resource-burning defense requires a means to verify both that the claimed resource was consumed and which identifier consumed it — the paper's own definition makes this a hard requirement, not an implementation detail. Requires the resource's cost to be arbitrarily tunable per identifier (a challenge of difficulty x consumes x units), which the paper states holds naturally for computation, memor |
| `GYONGYI-VLDB-04` | F | Combating Web Spam with TrustRank | Requires a directed link graph over the items to be ranked (the paper uses web hyperlinks; any analogous directed graph would substitute) and requires that "good" nodes point predominantly to other good nodes (the "approximate isolation" assumption) — TrustRank's demotion of spam depends on spam sites lying multiple hops from every seed, so a spam item with a direct or short-hop link from a seed p |
| `HALE-SSR-26` | H | Benchmarking of the Amortized Post Quantum Combiner for MLS | Requires maintaining two parallel MLS sessions with identical membership at all times, exactly as in GREENE-EPRINT-25; any membership-management component must apply every Add/Remove to both sessions. Requires the delivery/ordering layer to carry two independent commit streams (traditional and PQ) plus the PSK Proposal message that ties a Full Commit's PQ secret into the traditional session, at a  |
| `HALKES-NETWORKING-11` | L | UDP NAT and Firewall Puncturing in the Wild | A design relying on this paper's rendezvous mechanism needs a third peer already holding an established connection to both endpoints that want to connect, so a directly-connectable-peer population (this paper measures it at 21% of the total in Trial 1) must exist for rendezvous relaying, or another means of introducing peers to a shared connected third party. NAT/firewall type self-classification  |
| `HANDSHAKE-WP-18` | E | Handshake: A Decentralized, Permissionless Naming Protocol Compatible with the Domain Name | A client that wants root-zone security without a full copy of the chain needs an SPV resolver capable of verifying FFMT inclusion and exclusion proofs; the whitepaper states the protocol is unusable without this. For every TLD not yet claimed by its legacy holder, an SPV client is stated to require either a hardcoded ICANN root zonefile snapshot or a live query path to ICANN's own root servers as  |
| `HARISH-NETWORKING-26` | D | ListGuard: Mitigating Pollution Attacks on In-Network Invertible Bloom Lookup Tables | ListGuard requires a companion item counter to be maintained in the data plane alongside the IBLT, incremented on every insertion; the recovery threshold decision depends on comparing this counter against the number of flows the peeling process actually extracts, so any deployment must add this second data-plane primitive, not just the IBLT. It requires the flow sampler to run inline with insertio |
| `HASHIMOTO-CCS-22` | H | How to Hide MetaData in MLS-Like Secure Group Messaging: Simple, Modular, and Post-Quantum | Requires every group member to hold and continuously evolve a single shared group secret key, as in MLS-style CGKA (not per-pairwise secrets, as in Signal); the wrapper's server-authentication mechanism is built directly on proving knowledge of this key each epoch. Requires the underlying CGKA to already realize the paper's F^ctxt_CGKA functionality (hiding layers 1 and 2); the wrapper adds only l |
| `HE-IMC-23` | K | Flocking to Mastodon: Tracking the Great Twitter Migration | A follow relationship in this federation design requires the follower's home instance to perform and maintain a subscription to the followed account's home instance on the user's behalf; this is what makes the federated timeline exist as the union of all posts any local user's following relationships have pulled in, and a client or protocol design that skips per-instance subscription bookkeeping c |
| `HEEB-ARXIV-24` | F | Sybil Detection Using Graph Neural Networks | Requires a small set of already-known, correctly-labeled honest and Sybil nodes (5% of each region's true size by default) before either pre-training or transductive inference can proceed; SybilGAT does not itself determine which nodes to trust as ground truth. Requires, for the pre-training mode that is the paper's primary operating mode, access to a smaller sample subgraph of the same or a simil |
| `HEEP-ATNAC-10` | A | R/Kademlia: Recursive and Topology-aware Overlay Routing | - Requires hop-by-hop hop failure detection (the TCP-style RTT-based timeout) to be computable at every node on the routing path, which requires each node to keep round-trip-time mean and variance estimates for its k-bucket peers. - Requires, for PNS, that every newly met candidate node be latency-probed before insertion into a k-bucket; a network coordinate system cannot substitute for this becau |
| `HEGEDUS-ECMLPKDD-19` | K | Decentralized Recommendation Based on Matrix Factorization: A Comparison of Gossip and Fed | Gossip learning as evaluated here requires a membership/peer-sampling service to already exist and supply each device with addresses of other currently live devices; the paper is explicit that this is assumed rather than provided by the mechanism under test, and names three possible external implementations (centralized service, dynamic decentralized protocol, or a static NAT-capable random-neighb |
| `HEI-TMM-07` | K | A Measurement Study of a Large-Scale P2P IPTV System | A crawler-based measurement of this kind requires the target system's peer-discovery protocol to be gossip-based and to expose peer lists on request, without access-control or rate limiting that would prevent an unauthenticated third party from iteratively harvesting the full peer set; the paper's own methodology exploits exactly this property of PPLive and states an independent third party can re |
| `HEILMAN-USENIXSEC-15` | A | Eclipse Attacks on Bitcoin's Peer-to-Peer Network | The attack and its analysis assume the victim node eventually restarts; the paper argues this assumption is realistic (citing a measured 25% chance of a public-IP node going offline within 10 hours, forced software updates, and DDoS-inducible restarts) rather than treating it as a special case, so any system relying on connection-table integrity across the node's lifetime must account for restart  |
| `HENZINGER-USENIXSEC-23` | G | One Server for the Price of Two: Simple and Fast Single-Server Private Information Retriev | A deployment must give every client an offline channel to download the per-database hint (121 MB for a 1 GB database under SimplePIR, or 16 MB independent of record count under DoublePIR for one-byte records) before that client's first query, and must re-distribute an updated hint whenever the underlying database's content changes materially enough to invalidate the old hint's precomputed structur |
| `HERRMANN-P2P-14` | B | Censorship-Resistant and Privacy-Preserving Distributed Web Search | - The node density protocol requires an ID-generation scheme (cited separately, not built here) that prevents an attacker from freely choosing arbitrary node IDs; without it, an attacker can still place IDs precisely enough to defeat the density check, as demonstrated for YaCy and Faroo's current unrestricted self-chosen IDs. - The node density protocol requires a network-wide consistent value for |
| `HOFFMAN-CSUR-09` | F | A Survey of Attack and Defense Techniques for Reputation Systems | A reputation system built using this framework requires an authentication mechanism external to the reputation logic itself to bind feedback to the identity that produced it; the paper states systems lacking data authentication and integrity checks cannot distinguish fabricated from legitimate feedback and are vulnerable to self-promotion and slandering as a direct consequence. Systems relying on  |
| `HOLLAND-POPETS-22` | G | RegulaTor: A Straightforward Website Fingerprinting Defense | Both the client and the entry-side proxy (in the paper's implementation, a Tor bridge running a pluggable transport) must run the RegulaTor scheduler in lockstep: the upload-side schedule is derived from the observed download-side sending rate (rate = download rate / U), so the proxy's download pacing must be visible to, or coordinated with, the client's upload pacing logic. The defense pads traff |
| `HU-SP-21` | E | Merkle^2: A Low-Latency Transparency Log System | The construction assumes the server picks the maximum chronological tree height (and thus maximum log capacity) before deployment, since the pre-build technique that keeps append cost bounded depends on the height being fixed in advance; a component needing unbounded future capacity must plan a height large enough in advance or accept a redeployment when the cap is reached. Concurrency control req |
| `HUANG-ATC-12` | C | Erasure Coding in Windows Azure Storage | Every fragment belonging to one coding group must be placed in a distinct fault domain; the reliability figures in Table 1 and the correlated-failure caveat both depend on this placement holding, since a shared fault domain across group members would violate the model's independence assumption and invalidate the stated MTTF. The stream layer must support an asynchronous, off-the-critical-path back |
| `HUGENROTH-USENIXSEC-21` | H | Rollercoaster: An Efficient Group-Multicast Scheme for Mix Networks | Requires an underlying Loopix-model mix network already deployed and correctly parameterized (fixed per-client Poisson rates for payload, drop, and loop messages; per-hop exponential delay; a layered mix topology with providers as user entry points); Rollercoaster is a layer on top of this network and supplies no mix-network mechanism of its own. |
| `HULSING-CCS-19` | E | The SPHINCS+ Signature Framework | A verifier must have access to and trust the public parameters (a public seed, or a larger public-parameter structure depending on the robust/simple construction choice) associated with a given public key; the security reduction is stated relative to those parameters being generated as specified, not arbitrarily. The deterministic default signing mode requires the signer to hold a secret PRF key ( |
| `IDE-ARXIV-25` | E | Personhood Credentials: Human-Centered Design Recommendation Balancing Security, Usability | A tiered or multi-factor personhood-verification design, as the paper's own recommendation, requires the rest of the system to let a user or a relying party select among data sources of different sensitivity per transaction context, since participants expressed a clear preference against a single fixed verification method applied uniformly across financial, healthcare, social-media, and government |
| `IDENA-WP` | E | Idena: Proof-of-Person Blockchain Technology Overview | Global time synchronization across all participants is a precondition the whole mechanism depends on: the document states uniqueness follows specifically from every participant decrypting flips "at the same time worldwide," so any client or network layer that lets a subset of participants receive or answer flips outside the shared narrow window (through relayed decryption, replay, or clock skew) r |
| `ISMAIL-BLED-02` | F | The Beta Reputation System | Requires an external identity/authentication mechanism binding accumulated feedback to a persistent identity, because the reputation function's counts (r, s) are keyed to whatever identifier the rest of the system supplies, and the paper explicitly declines to defend against identity change. Requires an external feedback-collection and delivery channel that gets each transaction's positive/negativ |
| `JACOB-JACM-14` | A | SKIP+: A Self-Stabilizing Skip Graph | Every node must have an immutable, globally unique identifier and an independently, uniformly randomly chosen bit string of sufficient length assigned at join time; the self-stabilization proof (Lemma 3.1 onward) depends on these random strings to bound node degree and convergence time, so a system that assigns non-random or attacker-chosen bit strings to nodes voids the stated bounds. The synchro |
| `JACOB-MIDDLEWARE-19` | J | A Glimpse of the Matrix: Scalability Issues of a New Message-Oriented Data Synchronization | Homeserver-to-homeserver load balancing by relocating users cannot be used as a mitigation, because Matrix homeservers hold only limited mutual trust and a receiving server cannot compel a sending server's user population to redistribute. Any group communication mechanism substituted for Matrix's current per-room full broadcast needs, at minimum, per-room visibility into which servers participate  |
| `JACOB-SACMAT-20` | J | Matrix Decomposition: Analysis of an Access Control Approach on Transaction-based DAGs wit | States four properties that any authorization-policy set and conflict-resolution mechanism on a TDAG must satisfy to be secure under this analysis: the algorithms must be deterministic and depend only on data already present in the DAG, so that every consistent replica computes the same decision from the same data; a subject must be unable to acquire a permission it was not granted by another subj |
| `JAEGER-EUROCRYPT-25` | H | Analyzing Group Chat Encryption in MLS, Session, Signal, and Matrix | For a symmetric-signcryption-based chat-encryption construction of the kind analyzed here to achieve full IUF and OAE security (not merely the weakened, attack-excepted versions each of the four deployed protocols is proved to meet), the paper's own two secure reference constructions (from the underlying JKS24 work) require the signature to explicitly bind the group (key) identifier, the nonce, an |
| `JANSEN-HOTPETS-14` | I | From Onions to Shallots: Rewarding Tor Relays with TEARS | Requires a separate, unspecified secure bandwidth-auditing subsystem that reports per-relay contribution to the bank; the paper does not supply this component. Requires a Byzantine-fault-tolerant agreement protocol (the paper cites Fast Paxos) among a quorum of semi-trusted bank servers to order transactions, plus a threshold-signature scheme (cited: Pedersen; Boldyreva) so a majority of servers c |
| `JANSEN-PAM-21` | I | On the Accuracy of Tor Bandwidth Estimation | A capacity-ordered overlay that assigns relay selection weight from a relay's own self-reported throughput requires that a relay actually attract enough real traffic to reach its true capacity within the self-measurement window (5 days here); a relay that has not yet attracted proportional traffic reports a capacity below its true value, and the paper's central measured finding is that most relays |
| `JARECKI-ASIACRYPT-14` | I | Round-Optimal Password-Protected Secret Sharing and T-PAKE in the Password-Only Model | The V-OPRF-based T-PAKE requires servers to use independent per-client V-OPRF keys — the paper states explicitly that adding the client identity as an input to a shared V-OPRF key would not achieve the same guessing-attempt-counting property, so key derivation per client, not per-request tagging, is required. The scheme assumes a common reference string (CRS) is available to all parties before any |
| `JEGOU-TPAMI-11` | B | Product Quantization for Nearest Neighbor Search | - IVFADC requires two separate k-means-trained codebooks in place before any indexing occurs: a coarse quantizer (k' centroids) and a product quantizer trained specifically on the residual vectors that result from the coarse quantizer's assignment — the product quantizer cannot be trained independently of the coarse quantizer's output distribution. - The coarse quantizer's centroid count k' and th |
| `JOHNSON-CCS-13` | J | Users Get Routed: Traffic Correlation on Tor by Realistic Adversaries | A capacity-weighted relay-selection mechanism that keeps a client's entry point fixed for tens of days (Tor's guard rotation, measured here at a 30-60 day tenure) concentrates deanonymization risk on whichever party is chosen during that tenure, so any design reusing guard-style fixed-entry selection must treat the guard-rotation interval as a directly load-bearing security parameter, not a perfor |
| `JOHNSON-POPETS-17` | I | PeerFlow: Secure Load Balancing in Tor | PeerFlow requires a functioning Directory Authority infrastructure that collects, trims, and aggregates peer-submitted measurement statistics into consensus weights, and that runs the probation and per-period weight-increase-cap logic — a design with no analogous centralized aggregation point cannot apply this mechanism as specified. It requires each relay in the "measuring" subset (the largest `µ |
| `JUAREZ-ESORICS-16` | G | Toward an Efficient Website Fingerprinting Defense | WTF-PAD requires a padding endpoint (a Tor pluggable transport instance) at both the client and the bridge/guard, each running its own AP state machine pair (forward and receive), so the mechanism cannot be deployed unilaterally by the client alone — the paper's own threat model places the observation point between client and bridge/guard, and the defense pads exactly that hop. Because the PT clie |
| `JUELS-CCS-07` | C | PORs: Proofs of Retrievability for Large Files | The verifier must generate and retain the sentinel-generation key and the permutation key; loss of these keys makes future challenges and file recovery impossible, since sentinel positions and values are derived from them. The verifier (not the archive) must perform the initial encoding — error-correction, encryption, sentinel insertion, permutation — before handing the file to the archive, so the |
| `JUELS-NDSS-99` | I | Client Puzzles: A Cryptographic Countermeasure Against Connection Depletion Attacks | The server must hold a secret value used to construct puzzle inputs, and must know the current time, since both feed the one-way function that produces each puzzle; compromise of that secret lets an adversary precompute or verify puzzle solutions off-protocol. The server must maintain a buffer of maxcon + b slots and enforce the three timeout windows (T1, T2, T3) on every pending connection, purgi |
| `KAASHOEK-IPTPS-03` | A | Koorde: A Simple Degree-Optimal Distributed Hash Table | Koorde requires Chord's ring-maintenance machinery to already exist and function correctly: node identifiers placed on a consistent-hashing ring, a working join protocol, and (for the fault-tolerant variant) Chord's successor-list stabilization protocol, since the paper states Koorde reuses these directly rather than defining new ones. The de Bruijn forwarding step requires that each node's succes |
| `KAKHKI-IMC-17` | L | Taking a Long Look at QUIC: An Approach for Rigorous Evaluation of Rapidly Evolving Transp | Any comparison of QUIC against TCP that claims fairness must control for competing-flow count and buffer size explicitly, because this paper found QUIC consumes roughly double a single competing TCP flow's throughput and remains over half of a shared link's bandwidth against as many as 4 competing TCP flows, under the same Cubic congestion-control algorithm on both sides — a system relying on TCP- |
| `KALODNER-WEIS-15` | E | An Empirical Study of Namecoin and Lessons for Decentralized Namespace Design | A consuming application needs continuous access to a synchronized copy of the Namecoin block chain to resolve a name, because the mapping lives only in chain state, not on any server the application can query directly. Renewal is a proof obligation on the holder: a name goes back to the available pool automatically after 36,000 blocks (~250 days) without an on-chain NAME_UPDATE citing a still-vali |
| `KAMVAR-WWW-03` | F | The Eigentrust Algorithm for Reputation Management in P2P Networks | EigenTrust requires a pre-existing, small, out-of-band-designated set of pre-trusted peers whose trustworthiness is assumed rather than derived from the algorithm itself; the paper suggests network designers or founders fill this role but gives no verification procedure. It requires a functioning distributed hash table (CAN or Chord are named) to locate each peer's score managers by hashing its un |
| `KANARIS-ARXIV-23` | L | Mass Adoption of NATs: Survey and experiments on carrier-grade NATs | Two-sided EIM-to-EIM puncturing, as described here, requires a synchronizer or rendezvous server both peers can reach, to exchange externally observed addresses and coordinate a simultaneous outbound packet from each side; the paper states both peers must initiate communication at the same time, so the mechanism further needs a shared clock or synchronization signal between the two sides. Brute-fo |
| `KARAMSHUK-INFOCOM-15` | K | ISP-friendly Peer-assisted On-demand Streaming of Long Duration Content in BBC iPlayer | A design reusing this "online while you watch" peer-availability model requires an enforcement mechanism ensuring a peer that stops watching also stops being counted as available to serve, since the reported traffic gains collapse by more than half once peers are permitted to depart immediately after their own download completes (the "online while downloading" sensitivity test). The paper's own pr |
| `KEIZER-CSUR-24` | B | A Survey on Content Retrieval on the Decentralised Web | As a survey, this paper does not itself impose requirements on a system built to its recommendations, but its classification of the mechanisms it reviews makes explicit several composition requirements a designer must satisfy to use them. Content-addressed storage (self-certifying CIDs) requires that any component providing a "current version" or "latest state" abstraction over that storage supply |
| `KEIZER-MOBIHOC-20` | L | Rewarding relays for decentralised NAT traversal using smart contracts | The Proof of Timely Relay mechanism requires the client to have simultaneous, independent contact with two separate relay-capable nodes for every relay session — a working relay and a check node — so a network with too few reachable, relay-capable nodes near a given client cannot supply the second, independent verification path this mechanism depends on. The smart-contract payment-release logic re |
| `KENIAGIN-SIGMETRICS-25` | D | CertainSync: Rateless Set Reconciliation with Certainty | Both participants must independently construct their own IBLTs using the identical rateless matrix (same construction type and same row ordering) before any subtraction step is meaningful, since the symmetric-difference IBLT is obtained only by subtracting two IBLTs built under the same element-to-cell mapping. The receiving participant must be able to attempt listing after each round and to signa |
| `KERANEN-RFC-18` | L | Interactive Connectivity Establishment (ICE): A Protocol for Network Address Translator (N | ICE itself supplies no mechanism for exchanging candidates between the two agents; it depends on a separate signaling channel (the RFC names SIP offer/answer as the typical example) that both agents can already reach, and the RFC states explicitly that "ICE is not intended for NAT traversal for the signaling protocol, which is assumed to be provided via another mechanism." The RFC requires, not me |
| `KERMARREC-DICG-20` | J | Gossiping with Append-Only Logs in Secure-Scuttlebutt | Every log requires a durable per-store persistence layer with no eviction, because the reconciliation protocol (Algorithm 1) assumes each store retains every event of every log it holds; the paper's frontier-difference computation is undefined for a store that has evicted events. Reliable, ordered delivery between two connected stores is required at the transport layer, since the design explicitly |
| `KLEIN-SP-21` | H | Keep the Dirt: Tainted TreeKEM, Adaptively and Actively Secure Continuous Group Key Agreem | Requires an untrusted delivery server that every party sends Update/Add/Remove operations to and that enforces a single global order on those operations across all group members — when two parties submit conflicting operations concurrently, the server's ordering decision is what each party's confirm/reject response is built from. The server can prevent communication (withhold delivery) but the pro |
| `KLEPPMANN-AFP-18` | D | OpSets: Sequential Specifications for Replicated Datatypes | The specification requires every operation to carry a globally unique identifier from a total order, generated locally without any node needing to coordinate with any other node to obtain it; a system supplying only per-node monotonic counters without a tie-breaking node identifier would not satisfy the uniqueness requirement. It requires the underlying transport to guarantee, at minimum, eventual |
| `KLEPPMANN-ARXIV-20` | D+H | Byzantine Eventual Consistency and the Fundamental Limits of Peer-to-Peer Databases | Every replica must hold a distinct private key for digital signatures, with every other replica's corresponding public key known to it; every message must be signed by its sender, and a recipient discards any message with an invalid signature — a deployment without this signing infrastructure cannot use the algorithm, since it relies on signatures to prevent a faulty replica from impersonating a c |
| `KLEPPMANN-CONEXT-24` | J+K | Bluesky and the AT Protocol: Usable Decentralized Social Media | A Relay requires every PDS to serve a WebSocket stream of signed repository updates and to accept periodic re-crawl requests as a fallback when the stream is interrupted; without a re-crawl fallback, deletions and additions made during a network interruption are missed. An App View requires a Relay's firehose as its only input and does not itself crawl PDSes; a new social mode built on the protoco |
| `KLEPPMANN-ONWARD-19` | D | Local-First Software: You Own Your Data, in Spite of the Cloud | For the seamless-collaboration ideal to hold without a central server, the paper's prototype experience found that merge behavior needs to operate at a fine granularity that accounts for a datatype's own semantics — its example is that Automerge positions two users' concurrent insertions into the same array position deterministically, whereas a line-oriented tool like Git treats the same situation |
| `KLEPPMANN-PAPOC-19` | D | Interleaving Anomalies in Collaborative Text Editors | Ruling out the full interleaving anomaly under the paper's revised specification (clause 1(d)) requires the merge algorithm to preserve, for every pair of concurrently inserted sequences occupying the same gap, a total group ordering between the two sequences as wholes — an algorithm that assigns identifiers to individual characters independently of which other characters were inserted in the same |
| `KLEPPMANN-PAPOC-22` | D+H | Making CRDTs Byzantine Fault Tolerant | Every node must store the complete DAG of updates it has delivered (all vertices and edges), not a summary, since validity and delivery decisions read the full causal history of an update through `before(u)`. The system needs a way to encode each update as a byte string before computing its hash, and operation-based CRDT implementations must follow the generator/effector split (an operation is gen |
| `KLEPPMANN-TPDS-17` | D | A Conflict-Free Replicated JSON Datatype | Every replica needs a transport that eventually delivers every generated operation to every other replica; delivery order across the network need not be causal, but the replica must buffer any operation whose `deps` are not yet all locally applied and apply it only once they are — so the replica-side apply logic, not the network, is what enforces causal order. Each replica needs a stable, comparab |
| `KLEPPMANN-TPDS-22` | D | A Highly-Available Move Operation for Replicated Trees | The transport must deliver every operation to every other replica eventually, but need not preserve delivery order — the replica's own undo-do-redo application logic reorders operations into timestamp order regardless of arrival order — and the paper explicitly assumes an underlying network protocol detects and retransmits lost messages and suppresses duplicates (i.e., reliable, deduplicated deliv |
| `KOLESNIKOV-CCS-16` | G | Efficient Batched Oblivious PRF with Applications to Private Set Intersection | Requires both parties to run in a semi-honest (honest-but-curious) threat model; the construction offers no defense if either party deviates from the protocol, so a caller needing security against an actively malicious counterparty cannot use this construction as specified. Requires a private symmetric channel over which to run OT-extension messages (base OTs plus subsequent symmetric-key rounds); |
| `KOMLO-SAC-20` | E | FROST: Flexible Round-Optimized Schnorr Threshold Signatures | FROST requires a location (a "commitment server" or equivalent) where participants publish their preprocessing-stage nonce commitments for the aggregator to consume; the paper states this location need not be trusted for confidentiality (public commitment values grant no extra power to an adversary who reads them) but must be trusted to serve correct, unused values, since a malicious commitment se |
| `KONG-TIT-24` | C | Locally Repairable Convertible Codes With Optimal Access Costs | A storage system adopting this code-conversion scheme must be able to select, at conversion time, evaluation points and a finite field of the size stated in Corollary III.2, tied algebraically to the specific (k, r, l_I, l_F, ζ) parameters chosen — the construction is not parameter-agnostic, and a system wanting to convert between arbitrary rate targets must verify the required field-size and divi |
| `KREUTER-CRYPTO-20` | I | Anonymous Tokens with Private Metadata Bit | The scheme requires a random-oracle-model hash function `H_t` mapping arbitrary strings to group elements (instantiated here via Elligator 2 with SHA-512), and a second random oracle `H_s` used to derive the shared per-issuance randomness `S`; both are treated as ideal random oracles in the security proofs, not merely collision-resistant hashes. Unforgeability rests on the Chosen Target Gap Diffie |
| `KROL-EUROSP-24` | A | DISC-NG: Robust Service Discovery in the Ethereum Global Network | DISC-NG requires an underlying Kademlia-style DHT whose own routing-table maintenance already resists eclipse attacks, and cites specific such mechanisms as preconditions rather than building one itself. It requires every advertisement to carry a verifiable digital signature — the lookup algorithm asserts `ad.hasValidSignature()` before accepting an advertisement — so a signing key-management sche |
| `LACAVA-ANS-21` | J | Understanding the growth of the Fediverse through the lens of Mastodon | Reproducing or extending this paper's Instances-network measurements requires an authenticated Mastodon API token from each instance to be crawled, since the authors state they deliberately excluded any instance that declined to grant a token rather than scraping it by other means; a study that instead scrapes public HTML would collect a different, non-directly-comparable instance population. Comp |
| `LANGLEY-SIGCOMM-17` | L | The QUIC Transport Protocol: Design and Internet-Scale Deployment | QUIC's 0-RTT handshake requires the client to have completed at least one prior successful handshake with the specific server (or, more precisely, the specific data center's server configuration) it is reconnecting to, and to still hold both the cached server config and a still-valid source-address token from that prior handshake; a client reconnecting to a server it has never contacted, or whose  |
| `LARSEN-CRYPTO-18` | G | Yes, There is an Oblivious RAM Lower Bound! | Any component in the target architecture that stores client data on an untrusted peer or server and wants access-pattern privacy through ORAM inherits a floor: retrieving one r-bit block from an n-block online ORAM under bounded client memory costs at least a constant times lg(nr/m) times as much server-side traffic as an access-pattern-revealing read would, and this floor holds even for computati |
| `LAS-EPRINT-26` | F | De-SyRIS: A Decentralized Sybil-Resistant Identity System without a Trusted Credential Iss | Requires at least one operational TEE deployment (the paper specifies Oasis ROFL/Sapphire specifically) that can produce a remotely-attestable computation result; the paper's own discussion section states this is a live risk, not a design abstraction — it cites the 2025 SGX WireTap DRAM-bus-interposition attack (Seto et al., CCS 2025) as the reason the Integritee TEE-based project was discontinued |
| `LAZAR-OSDI-18` | G | Karaoke: Distributed Private Messaging Immune to Passive Traffic Analysis | Requires every server on a message's chosen path to participate honestly in decrypting and re-shuffling within its assigned layer; a message cannot be decrypted by an adversary who does not control every server on its path, but the overall privacy guarantee assumes a globally known, fixed set of N servers with h of them honest, so the system needs a mechanism (not supplied by this paper) to establ |
| `LAZAR-SOSP-19` | G | Yodel: Strong Metadata Security for Voice Calls | Requires an external metadata-private messaging channel to bootstrap each call: the evaluation itself used Alpenhorn (this author group's own prior OSDI 2016 system) for out-of-band dialing and Karaoke (this author group's own prior OSDI 2018 system) for circuit-endpoint exchange, running on separate servers from the Yodel mixnet; Yodel supplies none of this bootstrapping itself. Requires synchron |
| `LESNIEWSKI-LAAS-NSDI-10` | F | Whanau: A Sybil-proof Distributed Hash Table | Requires a pre-existing social graph of honest, mostly-symmetric trust edges in which the honest region has no sparse cut internally and forms a fast-mixing expander graph; the paper's own measurement of this property (Figure 6) is a prerequisite check that must be re-run on any graph this is deployed against. Requires each node to know order-of-magnitude estimates of m (honest edge count), w (mix |
| `LESNIEWSKI-LAAS-SNS-08` | F | A Sybil-proof One-hop DHT | Requires a social graph, given to each node as its list of immediate neighbors, where all honest nodes hold locally generated unforgeable public/private keypairs and each node knows its immediate social friends' public keys through an out-of-band channel (for example physical rendezvous). Requires the social graph to be fast mixing (random-walk mixing time w = O(log n)); the paper asserts real soc |
| `LEVIEN-USS-98` | F | Attack-Resistant Trust Metrics for Public Key Certification | Requires every key that will be widely accepted to be certified by at least d other keys, where d directly sets the minimum number of keys or certificates an attacker must control; the paper states this outright as a precondition ("for an attack-resistant key infrastructure based on trust metrics to be viable, the owner of every key must be willing and able to have a number of people to certify it |
| `LEVIN-SIGCOMM-08` | I | BitTorrent is an Auction: Analyzing and Improving BitTorrent's Incentives | PropShare requires each peer to observe, per round, how much bandwidth each neighbor sent it in the prior round(s); it needs no information about neighbors' total capacity or their bids to other peers, unlike the best-response strategy from Feldman et al., which the paper states would require knowing each neighbor's total upload capacity and the sum of bids that neighbor received from everyone els |
| `LI-EPRINT-25` | A | A Place for Everyone vs Everyone in its Place: Measuring and Attacking the Ethereum Global | The attack requires that a node discovery protocol's passive-DHT-maintenance mechanism inserts any node record that pings it into a bucket or replacement slot without verifying the record's claimed service; removing that requirement (mandatory, verifiable service information at the record-insertion step, which is the paper's own section 6 proposal) would prevent the attack described here. The atta |
| `LI-INFOCOM-05` | A | A Performance vs. Cost Framework for Evaluating DHT Design Tradeoffs Under Churn | A protocol being compared under PVC must expose every tunable parameter that trades bandwidth for lookup performance (stabilization interval, routing-table size or base, lookup parallelism, gossip interval, or equivalent), because PVC's importance-ranking method depends on being able to fix one parameter and vary the rest; a protocol with an undocumented or hidden internal tuning knob cannot be fa |
| `LI-IPTPS-03` | B | On the Feasibility of Peer-to-Peer Web Indexing and Search | Partition-by-keyword requires a DHT that maps a word to the peer responsible for its posting list; the paper does not specify or evaluate which DHT, treating routing as already solved by cited prior systems (Chord, CAN, Pastry, Tapestry). The gap-compression and clustering optimizations require a periodic distributed renumbering step that remaps content-hash docIDs (160-bit) to dense sequential in |
| `LI-SIGIR-25` | B | Unsupervised Corpus Poisoning Attacks in Continuous Space for Dense Retrieval | A retrieval system's exposure to this attack requires that new documents can be added to the searchable corpus without a human or automated relevance check gating whether an embedding-similar, token-dissimilar document reaches a high rank. The attack requires white-box access to the encoder weights and similarity function; a system that keeps its encoder private denies the primary threat model tes |
| `LIANG-ARXIV-24` | L | Implementing NAT Hole Punching with QUIC | A third-party relay server, reachable by both endpoints before their direct connection exists, must be available to exchange each side's NAT-mapped public address; the relay is required for connection setup and, separately, for connection-migration restoration (to re-exchange the changed address), but not for data transmission once punching succeeds. The mechanism requires the local NAT to run End |
| `LIN-ICDEW-21` | J | Measuring Decentralization in Bitcoin and Ethereum using Multiple Metrics and Granularitie | This is a measurement study of two already-deployed Proof-of-Work blockchains and places no mechanical requirement on other system components; it is evidence about deployed decentralization outcomes, usable to compare against a design's own stated production-diversity target. A design that wants to reproduce or extend this measurement needs an equivalent block-producer identity signal; the paper c |
| `LIU-PACMHCI-25` | K | Understanding Decentralized Social Feed Curation on Mastodon | braids.social is built entirely as a client against Mastodon's existing, unmodified server-side API; it requires no protocol change and no new server-side capability beyond what the Home Timeline, Public, and Trending Status endpoints already expose. It requires the home server to run the Trending Status endpoint (the paper notes this is itself a server-side re-ranking of the federated feed by pop |
| `LIVADARIU-INFOCOM-18` | L | Inferring Carrier-Grade NAT Deployment in the Wild | A design that needs to know whether a client sits behind CGN, such as an admission or reputation mechanism keying on IP address, cannot rely on passive external inference alone to identify individual CGN clients: both methods here operate at /24-block granularity and infer that an address block is used for CGN, not that any specific client address is currently behind one, and the paper notes CGN n |
| `LOCHER-HOTNETS-06` | I | Free Riding in BitTorrent is Cheap | For an incentive mechanism to withstand this attack, it cannot rely solely on a reciprocity check triggered only by past upload behavior, because BitThief supplies zero upload and still receives service through the optimistic-unchoke and seeder round robin paths, which are unconditional by design. Any defense that closes this path must either meter or price the optimistic-unchoke slot itself, or s |
| `LOO-IPTPS-04` | B | The Case for a Hybrid P2P Search Infrastructure | The hybrid scheme requires ultrapeers to participate in both a query-broadcast overlay and a DHT overlay simultaneously, and requires each ultrapeer to observe the result-set size of every query it forwards, so it can apply the small-result-set heuristic to decide what to publish. It requires a query-execution path capable of falling back from one search mechanism to the other within a bounded tim |
| `LUBY-FOCS-02` | C | LT Codes | - The encoder and decoder must agree on the same degree distribution (the Robust Soliton distribution, parameterized by k and δ) before encoding begins; this is stated as the only required preprocessing. - The decoder must be able to determine, for each received encoded symbol, its degree and its exact set of neighbor source-symbol indices — the paper requires this metadata to reach the decoder bu |
| `LUBY-RFC-11` | C | RaptorQ Forward Error Correction Scheme for Object Delivery | The transport must deliver each encoding symbol tagged with its Source Block Number and Encoding Symbol ID, since decoding depends on knowing which ESI each received symbol carries. Encoder and decoder must share the object-level Common FEC Object Transmission Information (F, T) and the scheme-specific parameters (Z, N, Al) before decoding starts; these are derived once from F and T by the fixed c |
| `LUO-ARXIV-25` | J | Unveiling Ethereum's P2P Network: The Role of Chain and Client Diversity | A shared node-discovery table (a single DHT serving Ethereum mainnet, Ethereum testnets, and unrelated EVM-compatible chains such as Polygon and BNB Smart Chain simultaneously, as measured here) requires an expensive per-candidate handshake (TCP connection, encryption, protocol Hello/Status exchange) before a peer's chain membership can be determined, so any design reusing a shared discovery layer |
| `MALKHI-PODC-02` | A | Viceroy: A Scalable and Dynamic Emulation of the Butterfly | - Requires that any server able to compute another server's identifier can open a connection to it; the paper treats connection establishment as a primitive and does not specify addressing, transport, or NAT traversal. - Requires non-overlapping join/leave events (a serialization or locking discipline elsewhere in the system) for the join/leave algorithms and their O(1)-expected-link-change bound  |
| `MALKOV-TPAMI-20` | B | Efficient and Robust Approximate Nearest Neighbor Search Using Hierarchical Navigable Smal | The search algorithm requires a fixed, designated entry point for the top layer of the graph; any system serving queries with HNSW must know or discover this entry point before it can start a search, and the entry point is updated whenever an inserted element's randomly drawn layer exceeds the current top layer. Distributing this index across separate machines (relevant for a decentralized deploym |
| `MALVAI-NDSS-23` | E | Parakeet: Practical Key Transparency for End-to-End Encrypted Messaging | Every client of the directory must independently obtain a small server commitment for each epoch from an out-of-band source it trusts to be consistent across all clients — Parakeet supplies this itself via its witness quorum-certificate mechanism, but any component depending on Parakeet must be able to reach that witness quorum (a majority of 2f+1 out of 3f+1 witnesses) rather than trusting the Id |
| `MANGIPUDI-EPRINT-26` | H | Auditable Continuous Group Key Agreement | Requires a public, append-only log maintained by a delivery service that every commit message is posted to, and against which any member or third party can check an escrow's proof without trusting the committer or the server — the paper's stateless verification model needs this log and nothing else. Requires the underlying protocol to be MLS-shaped: a TreeKEM update path producing an epoch root se |
| `MANKU-USITS-03` | A | Symphony: Distributed Hashing in a Small World | Symphony requires every node to be assigned a uniformly random real-numbered ring position at join time; the paper does not specify a Sybil-resistant or otherwise controlled identifier-assignment mechanism, so an external component must supply honest random-identifier assignment if the deployment needs one. The protocol requires each node to obtain an estimate of the current network size n (via th |
| `MARCUS-EPRINT-18` | A | Low-Resource Eclipse Attacks on Ethereum's Peer-to-Peer Network | A Kademlia-derived node-discovery layer that (like Ethereum's) does not require the "well-formedness" property of iterative content lookup — because content is replicated at every node rather than discovered by address — gains no benefit from keeping the node-ID-to-bucket distance mapping public, and a public mapping is what both the table-poisoning attack and, per the related-work section, the St |
| `MARTINS-JCC-26` | J | Matrix Protocol: A Comprehensive Systematic Mapping Study | This entry is a literature survey and places no mechanical requirement on a system design. Its value to the architecture synthesis is as a bibliography index over Matrix-specific primary studies (listed below) and as a map of which technical questions about federated real-time messaging remain open in the published literature as of this survey's search date, each of which would need its own full-t |
| `MATURANA-ISIT-23` | C | Locally Repairable Convertible Codes: Erasure Codes for Efficient Repair and Conversion | - The initial and final codes must both be systematic codes with (r, ℓ) data locality and optimal minimum distance, built from the paper's Vandermonde-based pyramid-code-plus-piggybacking construction; the technique is not stated to generalize to arbitrary existing LRCs. - A conversion changing k requires M := lcm(kI, kF) data nodes to be evenly divided among λI = M/kI initial codewords and λF = M |
| `MATURANA-TIT-22` | C | Convertible Codes: Enabling Efficient Conversion of Coded Data in Distributed Storage | Both endpoints (n_I, k_I) and (n_F, k_F) must be fixed and known to the code construction in advance for the access-optimal guarantee to apply as proven; the paper's own "Handling a priori unknown parameters" discussion shows the Hankel sequence can be built to stay access-optimal across a bounded family of possible final parameters (k_F' = lambda' * k_I, n_F' = r_F' + k_F', 0 <= r_F' <= r_F, 2 <= |
| `MAXWELL-DCC-19` | E | Simple Schnorr Multi-Signatures with Applications to Bitcoin | - Every signer must complete all three rounds — commit, reveal, partial-sign — with every other signer in a given signing session before the signature can be assembled; a signer that aborts after seeing others' revealed nonces (failing the commitment check) must cause the whole protocol run to abort, since the commitment step exists precisely to remove a signer's ability to choose its nonce share  |
| `MAYOR-P2P-13` | B | On Unstructured Distributed Search over BitTorrent | The mechanism requires every node to be reachable for direct point-to-point query and response (a node sending its torrent infohash to z randomly sampled peers and receiving either an empty response or a list of participant/indexer nodes), so it requires a random-peer-sampling capability at the network layer that can select z peers close to uniformly at random from the full node population; the pa |
| `MAZIERES-SIGOPSEW-98` | E | Escaping the Evils of Centralized Control with Self-certifying Pathnames | - A verifier (the SFS client) must already possess the self-certifying pathname string (Location and HostID) through some out-of-band channel before it can verify anything — the mechanism verifies a claimed public key against a name the client already holds, and states no discovery mechanism for finding an unknown server's correct HostID other than symbolic links, digitally signed email, or other  |
| `MAZZOCCA-COMST-25` | E | A Survey on Decentralized Identifiers and Verifiable Credentials | The architecture requires a VDR that is available whenever a verifier needs to resolve a DID or check an issuer's public key; the paper states verifiers can reduce live-lookup dependence by locally caching the public keys of issuers they already trust, but this is described as a mitigation, not a removal, of the availability requirement. It requires a functioning DID method adapter registered with |
| `MELARA-USENIXSEC-15` | E | CONIKS: Bringing Key Transparency to End Users | Every client needs persistent local storage across sessions to retain the most recent verified STR and to check monitoring proofs epoch to epoch; the paper states this explicitly as an assumption. Detecting a whistleblowable equivocation requires that at least one of a user's client devices retain network access the identity provider cannot fully block. Non-equivocation detection with cryptographi |
| `MENON-SP-22` | G | Spiral: Fast, High-Rate Single-Server PIR via FHE Composition | A deployment must supply and distribute Spiral's public parameters (14-47 MB depending on variant) to every client before that client's first query, in a phase the paper explicitly separates from the online per-query costs it measures; a design that cannot amortize this transfer over many queries per client will pay this cost on every query, unlike the comparison schemes' smaller 1-5 MB parameter  |
| `MENON-USENIXSEC-24` | G | YPIR: High-Throughput Single-Server PIR with Silent Preprocessing | A deployment adopting YPIR in place of SimplePIR/DoublePIR removes the requirement that clients fetch and cache a per-database hint before querying and that they re-fetch or patch that hint on every database update; this specifically removes the operational requirement Henzinger et al.'s SCT-auditing deployment imposes — maintaining multiple time-indexed copies of the database to serve hints for d |
| `MEYER-TR-24` | D | Range-Based Set Reconciliation Without Homomorphic Hashing | The backing tree data structure that both peers use must be history-independent (equal sets always produce structurally identical trees) and clamping-invariant, or the fingerprints two peers compute for the same subrange over the same items will not match even though the peers hold identical data in that range. Both peers must use the same clamping-invariant, history-independent data structure fam |
| `MICHEL-VLDB-05` | B | KLEE: A Framework for Distributed Top-k Query Algorithms | - Every cohort peer must maintain, per query term or attribute it indexes, a sorted index list plus a precomputed histogram-with-Bloom-filter structure (HistogramBlooms) over that list's score distribution; the paper notes this structure can be expensive to build and recommends precomputing and storing it locally rather than building it per-query, so a system adopting KLEE needs index lists that a |
| `MINSKY-TIT-03` | D | Set Reconciliation with Nearly Optimal Communication Complexity | - Both hosts must map their set elements into a common finite field F_q of size at least 2^b (b = element bit length), and must agree in advance on which evaluation points to use (or on a shared pseudo-random generator to produce them), so the field and evaluation-point selection are a pre-shared configuration, not negotiated per session. - The deterministic protocol needs a symmetric-difference b |
| `MISLOVE-HOTOS-03` | L | POST: A Secure, Resilient, Cooperative Messaging System | The mechanism requires a trusted certificate authority that signs a binding between each user's unique name (for example, an email address) and their public key, and separately issues the node-identifier certificates Pastry's secure routing depends on to resist adversarial node placement; POST assumes this authority exists and may set policy (for example, requiring each user's node identifier to b |
| `MISLOVE-NSDI-08` | F | Ostra: Leveraging Trust to Thwart Unwanted Communication | Requires an underlying trust network in which link formation and maintenance carries a real, non-zero cost to the participant, so that no user can cheaply create an unbounded number of identities or links; the paper states this property explicitly does not hold for the YouTube graph it evaluates against. Requires the trust network to be connected, so that a path exists between any two user identit |
| `MOHAISEN-IMC-10` | F | Measuring the Mixing Time of Social Graphs | - A Sybil-defense mechanism relying on a "fast mixing" precondition for its security proof needs the deployment's actual social graph measured for mixing time before that proof's guarantees can be trusted; this paper's own results show mixing time varies by more than an order of magnitude across graph types (100-400 for DBLP/Youtube/Facebook versus 1,500-2,500 for Livejournal, at the same epsilon= |
| `MUGHEES-CCS-21` | G | OnionPIR: Response Efficient Single-Server PIR | Requires the server to hold the full plaintext database and perform O(N) homomorphic operations (a dot product across all N entries, or the hypercube-decomposed equivalent) per stateless query, so server-side compute cost scales linearly with database size regardless of which entry is requested; a caller needing sub-linear per-query server computation must use the stateful variant or an orthogonal |
| `NASRULIN-BRAINS-22` | F | MeritRank: Sybil Tolerant Reputation for Merit-Based Tokenomics | MeritRank requires a gossip protocol (external to the reputation mechanism itself) that propagates feedback-graph updates (new edges, weight changes) between peers, and each peer's reputation computation operates only over that peer's own subjective, possibly stale or incomplete view of the graph — the mechanism does not require or produce global agreement on the graph state. It requires at least  |
| `NEAGUE-ARXIV-25` | B | Semantica: Decentralized Search using a LLM-Guided Semantic Tree Overlay | - Every peer must be able to compute or obtain an embedding for its own documents (locally via an LLM, or via an external embedding service) and for each incoming query, in a shared embedding space, before this mechanism can route anything. - The mechanism assumes perfect local search at each peer: the rest of the system must supply an exact-match or near-exact local retrieval routine over each pe |
| `NEGENTROPY-NIP77-23` | D | NIP-77: Negentropy Syncing (Nostr protocol extension for range-based set reconciliation) | Both participants must maintain their event sets as records addressable by a `(timestamp, 256-bit ID)` pair, sortable into one consistent total order (timestamp ascending, ID lexical ascending on ties), because the Range-based recursive splitting depends on both sides partitioning the identical ordered space identically. The reserved infinity timestamp value (max uint64) must never be assigned to  |
| `NGAN-IPTPS-03` | I | Enforcing Fair Sharing of Peer-to-Peer Resources | - Requires a public key infrastructure allowing every node to sign documents that any other node can verify and that others cannot forge; both the smart-card, quota-manager, and auditing designs assume this as given. - Requires the underlying peer-to-peer storage system to supply a mechanism for locating the replica holders of a given fileId (the paper cites PAST for this) since the remote list st |
| `OVEZIK-ACNS-25` | J | SoK: Measuring Blockchain Decentralization | Applying any of the paper's decentralization metrics to a resource requires the resource first be represented as a distribution of shares summing to a fixed total across a finite, enumerable set of parties (s_1...s_n); the framework explicitly cannot be applied as-is to a resource like network topology position that is not fungible in this sense. Computing a metric that is sensitive to zero-holdin |
| `OVEZIK-FC-24` | J | SoK: A Stratified Approach to Blockchain Decentralization | A layer's decentralization analysis under this methodology requires that the resource being tracked at that layer be identifiable and attributable to a controlling party; the paper's own Bitcoin case study reports several layers (Bitcoin hardware-by-manufacturer share, token ownership by wallet-software type) as inconclusive precisely because no such attribution data exists, which means any later  |
| `PAPAILIOPOULOS-TIT-14` | C | Locally Repairable Codes | The explicit MDS-point construction requires n to be arranged into groups of exactly r+1 blocks (r Reed-Solomon-coded blocks plus one XOR parity block per group), so a storage layer must place and address blocks by group membership for the stated single-block XOR repair procedure to apply; repairing a lost block requires simultaneously reading the r other still-live blocks of the same group, so an |
| `PATRA-TIT-25` | C | Generalized Regenerating Codes and Node Repair on Graphs | Repair requires the system to know, at the time of repair, the graphical distance from each candidate helper to the failed node, since the download-amount assignment tau: D -> [d] that determines each helper's beta_i is chosen based on that distance; the paper's bounds and constructions assume this distance information is available and correct. Intermediate Processing requires helper nodes along t |
| `PEDERSEN-CRYPTO-91` | E | Non-Interactive and Information-Theoretic Secure Verifiable Secret Sharing | - Requires a shared, agreed group setup — primes p, q with q / (p-1), and two generators g, h of the order-q subgroup G_q such that no party knows log_g(h) — established either by a trusted setup authority or by a coin-flipping protocol run by (some of) the participants before any secret is distributed; the security of the commitment scheme (and thus the whole verifiable-sharing protocol) depends  |
| `PERCIVAL-BSDCAN-09` | I | Stronger Key Derivation via Sequential Memory-Hard Functions | Any deployment of scrypt requires a general-purpose machine with random-access memory on the order of 1024*N*r bytes available during key derivation, since the mixing step must hold the full pseudorandom array to avoid the intended recomputation cost; a memory-constrained device (embedded hardware, a low-memory mobile client) cannot use large N without paying the same time cost the scheme imposes  |
| `PETIT-HUGUENIN-RFC-18` | L | Session Traversal Utilities for NAT (STUN) | A STUN Binding transaction requires a STUN server reachable by the client over the chosen transport (UDP, TCP, TLS, or DTLS) and requires that at least one NAT between client and server perform address/port translation on the outbound packet for the mechanism to produce a useful reflexive address; on a path with no NAT, the server simply returns the client's own address. Any usage that needs to tr |
| `PIATEK-NSDI-07` | I | Do Incentives Build Robustness in BitTorrent? | BitTyrant's unchoke-ranking mechanism requires an active set larger than the reference client's typical 50-100 peers to find enough high-value reciprocating partners; it achieves this by requesting more peers from the tracker and the DHT rather than needing any protocol change, so a system reusing this mechanism needs an underlying peer-discovery channel (centralized tracker, DHT, or both) capable |
| `PIETRZAK-ITCS-19` | I | Simple Verifiable Delay Functions | Requires a setup procedure — a trusted party or an honest-majority multiparty-computation ceremony — to generate the RSA modulus N without any party retaining or reconstructing its factorization; any subsequent use of the VDF for repeated challenges reuses the same N, so a single compromised setup compromises every future instance built on that N. Requires a cryptographic hash function to instanti |
| `PIOTROWSKA-USENIXSEC-17` | G | The Loopix Anonymity System | Loopix requires an external mechanism, not built in this paper, that lets a sender learn a receiver's provider IP address, per-provider client identifier, and public encryption key before a message can be sent — the paper names DP5 and MP3 as candidate "presence" systems but does not integrate one. It requires each user to register with exactly one provider, which must be honest for receiver unobs |
| `PIOTROWSKA-WPES-21` | G | Studying the Anonymity Trilemma with a Discrete-event Mix Network Simulator | Reproducing the Nym-favorable results requires a stratified topology with intersecting routes across layers, so that independent paths still contribute to one shared anonymity set — a P2P topology (as in HOPR) or a cascade topology (as in Elixxir) does not produce this property in the simulator. Sustaining Nym's entropy growth with user count as cover-traffic volume falls requires that cover traff |
| `POLINSKI-CCR-24` | K | The Centralization of a Decentralized Video Platform: A First Characterization Of PeerTube | PeerTube's redundancy mechanism requires a per-instance administrator to opt in explicitly by configuring a redundancy strategy and a storage quota; no redundancy occurs by default. It requires the origin instance to remain the sole authoritative holder of a video's mutable state — comments, view count — even after other instances hold full copies of the video bytes, so any consumer of view or com |
| `POST-NSDI-11` | F | Bazaar: Strengthening User Reputations in Online Marketplaces | Bazaar requires the marketplace operator to hold complete, authoritative knowledge of every transaction and its outcome (buyer identity, seller identity, monetary amount, and eventual feedback), because the risk network and its max-flow computation are built and queried centrally by that operator; the paper states explicitly that decentralizing this knowledge is unsolved. It requires a feedback ch |
| `QU-TOMCCAP-26` | J | Understanding User-Generated Content and Communities in Decentralized Web3 Networks: An Em | - A measurement or indexing component built on top of Nostr's relay layer needs to query multiple relays and deduplicate by event ID, because the same event is commonly mirrored across relays and no single relay is authoritative; this paper's own pipeline required exactly this deduplication step (latest-within-relay, earliest-across-relay) before any downstream analysis. - A component inferring "t |
| `QUELLE-PLOSONE-25` | K | Bluesky: Network topology, polarization, and algorithmic curation | The measurement method itself depends on two properties of the AT Protocol's decentralization design: a centralized DID-to-PDS directory (DID PLC) must be queryable to resolve any user's data-hosting location, and each user's personal data server must serve that user's repository to any requester who holds the user's DID, with no access control gating repository reads. Both properties are what mak |
| `RAMAN-IMC-19` | J+K | Challenges in the Decentralised Web: The Mastodon Case | A replication mechanism intended to improve content availability under instance or AS failure requires a global index reachable from any surviving instance, because a replica that exists only on instances that also fail, or that is invisible outside the instance that cached it (Mastodon's current subscription-cache behavior), does not restore availability; the paper's own subscription-replication  |
| `RASHMI-TIT-11` | C | Optimal Exact-Regenerating Codes for Distributed Storage at the MSR and MBR Points via a P | Repair and reconstruction both require linear operations over a fixed finite field Fq of the stated minimum size (2n for MBR, n^2 for MSR); a system layer that cannot perform field arithmetic at that size, or that needs to change n after code deployment beyond what the fixed field size supports, falls outside the construction as given. A failed node's repair connects to exactly d helper nodes chos |
| `RATNASAMY-SIGCOMM-01` | A | A Scalable Content-Addressable Network | Requires a uniform hash function mapping keys to points in the d-dimensional coordinate space, available to every node. Requires a bootstrap mechanism supplying a new node with the IP address of at least one existing CAN node (the paper assumes, as in prior work, an associated DNS mechanism for this). Requires nodes to send periodic soft-state update messages to their immediate neighbors carrying  |
| `RAWAT-DLT-24` | D | Accelerating Prolly Trees: Simplified Chunking for Rapid Updates | Every participant reconstructing or verifying the same key-value dataset must use the identical hash function (SHA-256, as used here) and the identical boundary-node threshold/`Q` parameter, because chunk boundaries and therefore the entire tree shape are derived deterministically from node-content hashes — two participants using different thresholds produce different, non-comparable trees for the |
| `REDDY-RFC-20` | L | Traversal Using Relays around NAT (TURN): Relay Extensions to Session Traversal Utilities  | The client must have some way, out of the scope of the TURN protocol itself, to communicate its relayed transport address to its peers and learn each peer's own server-reflexive transport address; the document names email exchange or a dedicated rendezvous protocol (ICE, with SIP carrying the ICE candidates in its message body) as examples, but the mechanism itself is external. Relaying data requi |
| `REED-SIAM-60` | C | Polynomial Codes Over Certain Finite Fields | Requires the sender and receiver to share the same finite field K of degree n over Z2 and the same generator beta (equivalently, the same irreducible polynomial f(x) of degree n) before any message is encoded or decoded. Requires the transmission channel to deliver each of the 2^n evaluated symbols tagged by, or otherwise associated with, its evaluation point (0, beta, beta^2, ...), because decodi |
| `REVUELTA-DLT-24` | I | Message Latency in Waku Relay with Rate Limiting Nullifiers | Every relaying node must maintain a locally synchronized copy of the current RLN Merkle tree of valid commitments, which requires either an Ethereum RPC provider connection (the paper names Infura and Alchemy as examples) or the node running its own Ethereum execution client; a node lacking this synchronization cannot validate incoming proofs correctly. The rate-limit and replay-detection mechanis |
| `REYNOLDS-MIDDLEWARE-03` | B | Efficient Peer-to-Peer Keyword Searching | - The mechanism requires a DHT layer beneath it to map each keyword to the node responsible for that keyword's document-ID list; the paper explicitly treats content/document placement as an orthogonal problem the DHT and storage layer solve separately, and assumes no benefit from co-locating a document with the keyword entries pointing to it. - Vertical partitioning requires that the number of par |
| `RHEA-USENIXATC-04` | A | Handling Churn in a DHT | A DHT that wants to use TCP-style per-neighbor timeout estimation for lookup messages needs recursive routing, because the technique depends on a node communicating almost exclusively with its own logarithmic-size set of direct overlay neighbors and maintaining a response-time history for each; a DHT using iterative routing (where the lookup source itself must estimate timeouts to arbitrary nodes  |
| `RICHARDSON-ECIR-13` | B | Ranked Accuracy and Unstructured Distributed Search | Rank-aware proportional and square-root replication (Section 3.2, Equations 11-12) require the system to compute, per document, the weighted retrieval rate v_i, which requires observing which query results (with their ranks) each document appeared in and at what query rate; this is the same "learn what a search found" signal square-root replication (Cohen and Shenker; Lv et al.) needs, extended he |
| `RICHARDSON-SIGIR-14` | B | Estimating Global Statistics for Unstructured P2P Search in the Presence of Adversarial Pe | Requires a secure, gossip-based random peer-sampling layer to select the z queried nodes per query and to bound the fraction of malicious nodes actually reachable by a query, because the entire accuracy analysis is conditioned on the assumption that node selection is unbiased; the paper cites Brahms as the concrete example and states Brahms's own 20% malicious-tolerance bound, not the statistics d |
| `RICHTER-IMC-16` | L | A Multi-perspective Analysis of Carrier-Grade NAT Deployment | Neither detection method requires cooperation from the ISP or CGN operator: the BitTorrent method depends only on the DHT protocol's existing peer-list propagation behavior and a crawler that can issue DHT queries and record replies; the Netalyzr method depends only on end users voluntarily installing and running the tool, and on the tool's own server distinguishing the packet's observed source ad |
| `RIPEANU-IPTPS-02` | B | Mapping the Gnutella Network: Properties of Large-Scale Peer-to-Peer Systems and Implicati | The traffic-proportional-to-connection-count relationship the authors rely on for their bandwidth estimate depends on the TTL-7, flood-based query-routing scheme in use at the time; the measurement and the resulting 330 TB/month estimate apply to that routing regime specifically and the paper does not claim the relationship holds under a different routing or replication scheme. The crawler's own m |
| `RODRIGUES-IPTPS-05` | C | High Availability in DHTs: Erasure Coding vs. Replication | Requires a data-placement mapping from current membership to the set of replica or fragment holders for each stored block, the paper cites consistent hashing (as used in CFS) as the example. Requires every node to detect, or be told, when a peer has been unreachable for longer than the membership timeout tau before that peer's data is treated as lost and repair bandwidth is spent, so the system ne |
| `ROOS-PETS-14` | J | Measuring Freenet in the Wild: Censorship-Resilience under Observation | A design relying on Freenet-style location-based routing requires the deployed long-range contact distribution to match the theoretical small-world model (Kleinberg-style, contact probability proportional to 1/distance) for the O(log^2 n)-style routing-length bound to hold; this paper measures the deployed distribution as uniformly random instead, which the paper's own simulation shows produces ro |
| `ROSENBERG-RFC-03` | L | STUN - Simple Traversal of User Datagram Protocol (UDP) Through Network Address Translator | The mechanism requires a STUN server reachable over UDP by the client and located in a public address realm that both communicating peers can treat as a common topological ancestor; if the client's application later shares its discovered mapped address with a peer, correct operation depends on that ancestor condition holding, which STUN itself has no way to verify or detect a violation of. The cla |
| `ROWSTRON-MIDDLEWARE-01` | A | Pastry: Scalable, Decentralized Object Location, and Routing for Large-Scale Peer-to-Peer  | Requires every node's nodeId to be drawn from a well-distributed 128-bit identifier space, and the paper states this is typically computed as the SHA-1 hash of the node's IP address or public key — whatever assigns nodeIds must produce a close-to-uniform distribution or the average routing-table population and hop-count bounds do not hold. Requires an application-supplied proximity metric function |
| `SAFARPOOR-DEHKORDI-AAMAS-25` | F | More Efficient Sybil Detection Mechanisms Leveraging Resistance of Users to Attack Request | Requires an ability to directly test a specific user's resistance to an attack request — the paper's operational definition is sending a friend request from a dummy Sybil account and observing whether it is accepted — which consumes a "budget" the paper treats as scarce (avoiding "bombarding all users"); the rest of the system must supply this test channel and bound its use. Requires a pre-existin |
| `SAFARPOOR-DEHKORDI-ARXIV-25` | F | Graph-Based Fake Account Detection: A Survey | Any detection method the survey covers requires that the deployer already possess a social graph with edges between accounts (following, friending, or message relationships) — none of the surveyed graph-based methods can classify an isolated account with no observed edges. Random-walk and belief-propagation methods additionally require a seed set of accounts already labeled real (and, for some var |
| `SAROIU-MMCN-02` | ? | A Measurement Study of Peer-to-Peer File Sharing Systems | A system that delegates routing, indexing, or content-serving responsibility uniformly across all participating peers requires the peer population's bandwidth, latency, availability, and shared-content volume to be roughly homogeneous; this paper's measurement shows those four properties vary across three to five orders of magnitude within a single deployed peer population, so any such system inst |
| `SATHIAMOORTHY-VLDB-13` | C | XORing Elephants: Novel Erasure Codes for Big Data | The light-decoder single-block repair path requires the block's full local repair group (the other blocks sharing its local parity) to be simultaneously available for download; the paper's Markov model explicitly computes separate probabilities for invoking the light decoder versus falling back to the heavy (full Reed-Solomon) decoder when more blocks are lost from a stripe than the local-parity s |
| `SECKIN-ARXIV-25` | J | The Rise of Bluesky | None stated as a mechanism requirement — this is a measurement study of an existing deployed system's growth and network structure, not a description of a mechanism another component could implement or depend on. Any citation of the giant-component or clustering-coefficient figures elsewhere in this corpus should be scoped to a federated/decentralized microblogging platform with open follow-graph  |
| `SHACHAM-ASIACRYPT-08` | C | Compact Proofs of Retrievability | The client must run an erasure-encoding step over the file before storage; the code's rate rho and the challenge parameters l and #B are jointly constrained so that epsilon-omega stays positive and non-negligible in lambda, so a system deploying this construction cannot pick rho, l, and B independently of each other. The verifier must be able to generate and hold either a private key (private-veri |
| `SHAMIR-CACM-79` | E | How to Share a Secret | Any system using this scheme needs a source of uniformly random field elements to generate the k-1 non-constant coefficients of q(x); the secrecy proof holds only under that uniformity assumption, so a biased or predictable coefficient generator narrows the equally-likely candidate set the proof relies on. Reconstruction requires each of the k combined pieces to carry its identifying index i along |
| `SHAPIRO-EATCS-11` | D | Convergent and Commutative Replicated Data Types | - CvRDT convergence requires the communication subsystem to deliver every update to every replica infinitely often (eventually, possibly with loss, reordering, or duplication tolerated); it does not require causal or any other ordering guarantee. A component selecting the state-based style needs only a best-effort anti-entropy or gossip transport, not a causal broadcast layer. - CmRDT convergence  |
| `SHAPIRO-SSS-11` | D | Conflict-Free Replicated Data Types | - CvRDT convergence (Theorem 1) requires only that the communication subsystem deliver every update to every replica eventually — no ordering, no exactly-once guarantee; the proof explicitly tolerates message loss, reordering, and duplication because LUB is idempotent and commutative. A component selecting the state-based style needs a fault-tolerant eventual-delivery transport (gossip or anti-ent |
| `SHEN-SP-24` | G | Real-Time Website Fingerprinting Defense via Traffic Cluster Anonymization | - Requires a Tor Pluggable Transport deployment point on both the client and a cooperating Tor middle node (or bridge), since the defense negotiates and applies packet regulation between those two parties; a system without a controllable relay/bridge component in the path cannot deploy this mechanism as specified. - Requires the client to know the destination website's identity before the connecti |
| `SHENG-NDSS-24` | I | Proof of Backhaul: Trustfree Measurement of Broadband Bandwidth | The mechanism assumes digital signatures are available to every challenger and verifiable by the prover and verifier, and it names Ed25519 specifically for its low computational overhead relative to signature size. It requires the verifier or a challenge coordinator to be able to select a fresh, randomly drawn subset of challengers per measurement from a larger active pool, so that a corrupted pro |
| `SHI-ARXIV-26` | C | Tight Lower Bounds and Optimal Constructions of Locally Repairable Convertible Codes in th | A system adopting this construction needs: (1) a coordinator process that can read from every initial-code storage node during conversion and write to every new storage node — the read-bandwidth accounting assumes a single coordinator performs the whole download, not a peer-to-peer diffusion; (2) the initial and final codes fixed to the "global split" shape, meaning the local-group size r and the  |
| `SHI-WWW-25` | A | Centralization in the Decentralized Web: Challenges and Opportunities in IPFS Data Managem | This is a measurement paper about an existing deployed system, not a mechanism proposal; it places no design requirement on a system being built. Its results place a requirement on how a design should reason about IPFS-as-a-component: a design that intends to rely on IPFS's DHT- and Bitswap-based content discovery for availability should not assume replication is high by default — the measured 29. |
| `SHI-WWW-26` | A | Eclipse Attacks on Ethereum's Peer-to-Peer Network | A discovery-table implementation that rate-limits incoming Ping messages per source (the paper's own proposed countermeasure) removes the precondition stage one depends on: an attacker's ability to send unlimited unsolicited Ping messages without being dropped. A DNS-based peer list that caps the number of distinct node records permitted per source IP address or subnet removes the precondition sta |
| `SHOKROLLAHI-TIT-06` | C | Raptor Codes | - The channel model assumed throughout is the binary erasure channel: symbols are either received intact or not received at all (erased), never corrupted; a system whose transport can deliver corrupted (not merely missing) data needs an additional integrity or error-detection mechanism upstream of this code, since Raptor decoding as specified here does not correct corrupted symbols, only recovers  |
| `SIDDARTH-FRONTIERS-20` | E | Who Watches the Watchmen? A Review of Subjective Approaches for Sybil-Resistance in Proof  | A reverse-Turing-test-based PoP mechanism (Idena's FLIP) requires a separate mechanism elsewhere in the system to stop one human from completing the test repeatedly under different identities, since the test by itself only distinguishes humans from machines, not one human from many. A web-of-trust vouching mechanism requires an existing social graph with enough density and diversity of connection  |
| `SILDE-FC-22` | I | Anonymous Tokens with Public Metadata and Applications to Private Contact Tracing | The key-derivation approach requires a public, deterministic function mapping a metadata string to a key-pair transformation, so that both the issuer and any verifying party can independently recompute the same derived public key from the base public key and the metadata value; a deploying system must therefore fix and publish this derivation function and treat the metadata string itself as public |
| `SINGH-ARXIV-26` | L | Lightweight Call Signaling and Peer-to-Peer Control of WebRTC Video Conferencing | The first signaling round-trip needs an asynchronous, out-of-band channel the browser tab did not open itself, because a browser tab cannot accept an unsolicited inbound connection; the implementation supplies this with Firebase Cloud Messaging push or with a human relaying an email/instant-message body, and states that a minimum of two messages (one per direction) is required for the offer-answer |
| `SINGH-INFOCOM-06` | A | Eclipse Attacks on Overlay Networks: Threats and Defenses | Every node needs a certificate binding its node identifier to a public key before the defense can be deployed; the paper does not supply or evaluate an identity-issuance mechanism, only assumes one exists. The overlay needs a working constrained-routing-table (CRT) secure-routing primitive as a fallback path used whenever primary PNS-based routing fails; the measured overhead figures (about 0.2 ms |
| `SIRINAM-CCS-18` | G | Deep Fingerprinting: Undermining Website Fingerprinting Defenses with Deep Learning | DF requires a labeled training dataset of traffic traces for every site the attacker wants classified in the monitored set, collected either from the same defense configuration the target will use or from data the attacker deliberately gathers under that defense; the closed-world evaluation retrains DF separately on defended and undefended datasets, so a defense's evaluation must specify whether t |
| `SIRIVIANOS-IPTPS-07` | I | Free-riding in BitTorrent Networks with the Large View Exploit | The exploit as measured requires only the standard tracker announce protocol and the standard practice, which the authors note is already implemented in mainstream compliant clients for tracker-failure resilience, of accepting and merging additional peer lists from other peers, so no protocol change or credential is required to mount it. The authors' proposed countermeasure requires every client a |
| `SIRIVIANOS-USENIXATC-07` | I | Dandelion: Cooperative Content Distribution with Robust Incentives | The provable non-manipulability property depends on each client holding one authenticated, paid account with the content provider; the paper states explicitly that "a user cannot boost its credit by presenting multiple IDs (Sybil attack) and claiming to have uploaded to some of its registered IDs" precisely because "each user maintains an authenticated paid account with the provider" and "essentia |
| `SIT-IPTPS-02` | A | Security Considerations for Peer-to-Peer Distributed Hash Tables | For principle 2 (observable lookup progress) to be enforceable, the lookup protocol must be iterative (each hop reports the next hop back to the querier) rather than fully recursive/relayed, because the paper states this check is impossible under CAN's proposed RTT-optimized recursive forwarding. For principle 3 (verifiable node identifiers) to be enforceable, the identifier-derivation function mu |
| `SRIVATSA-WWW-05` | F | TrustGuard: Countering Vulnerabilities in Reputation Management for Decentralized Overlay  | TrustGuard requires an underlying overlay network that is already secure against message misrouting and that ties every node identity to a single principal through digital certification or a secure join procedure; the thesis states this assumption explicitly and cites Douceur's Sybil attack result that a node able to spoof multiple identities amplifies its effective strength proportionally to how  |
| `STEFANOV-CCS-13` | G | Path ORAM: An Extremely Simple Oblivious RAM Protocol | Path ORAM requires the client to hold a stash (local storage measured empirically as usually near-empty, with worst-case size growing linearly in the security parameter lambda and, per the proof, O(log N) . omega(1) asymptotically) and, in the non-recursive construction, a position map whose size grows linearly with the total number of blocks N; the recursive construction removes this linear-growt |
| `STEINER-CCR-07` | A+J | Exploiting KAD: Possible Uses and Misuses | - The spying and eclipsing attacks both require the ability to have Sybil identities' presence inserted into victim peers' routing tables (via unsolicited "hello" messages accepted by KAD's own protocol); a routing-table admission policy that verifies a peer's identifier through means other than the peer's self-reported claim would remove this precondition. - The eclipsing attack requires the vict |
| `STEINER-IMC-07` | A+J | A Global View of KAD | A crawler-based measurement of a deployed Kademlia-family DHT requires the crawler to hold several hundred initial contacts to bootstrap discovery. Any protocol or client claiming persistent per-peer identity across sessions must be checked against measured behavior rather than assumed, because this paper finds that assumption false for a substantial and geographically concentrated minority of the |
| `STOICA-SIGCOMM-01` | A | Chord: A Scalable Peer-to-peer Lookup Service for Internet Applications | Chord resolves only to the responsible node for a key; the paper states explicitly that the correctness of a lookup under concurrent joins, leaves, and failures depends on maintaining accurate successor pointers via the periodic stabilization protocol, so any deployment needs that background process running continuously and needs to tolerate a bounded self-correction delay (governed by the stabili |
| `STRIBLING-IPTPS-05` | B | OverCite: A Cooperative Digital Research Library | Document-based (rather than keyword-based) index partitioning requires every query to be broadcast to all k partition-holding servers in parallel, so query cost scales with the number of partitions regardless of how few keywords the query contains; this is an explicit design tradeoff the paper makes to avoid keyword-partitioning's join cost, not a free choice. The DHT layer must support one-hop lo |
| `STUTZBACH-IMC-06` | A | Understanding Churn in Peer-to-Peer Networks | A component that assumes session lengths are exponentially or Poisson-distributed, to derive a churn rate or a routing-table refresh interval, is contradicted by this paper's fitted Weibull/log-normal distributions across all three systems measured; substituting the measured distribution requires re-deriving any such rate from the corresponding shape/scale parameters above rather than from a singl |
| `SUBRAMANYA-NEURIPS-19` | B | Rand-NSG: Fast Accurate Billion-point Nearest Neighbor Search on a Single Node | Requires a single physical machine holding both the RAM for the compressed-vector table and the SSD for the graph and full-precision vectors; the paper's mechanism is not distributed across machines, and the registry's own note (in `why_needed`) states the paper is being read as the baseline single-machine mechanism prior to evaluating any distribution proposal. Requires the full compressed repres |
| `SUN-ASONAM-20` | F | TrustGCN: Enabling Graph Convolutional Network for Robust Sybil Detection in OSNs | TrustGCN requires a signed graph in which edges record friend-request acceptance and rejection outcomes (or, for the unsigned-graph variant, a graph where the number of edges Sybils can forge to real users is limited); a system that only records final friendships and discards rejected-request events cannot supply this signal. It requires a pre-identified set of "trust seed" nodes already known to  |
| `SUN-CSCW-98` | D | Operational Transformation in Real-Time Group Editors: Issues, Algorithms, and Achievement | Every algorithm surveyed requires a full, causally-ordered replicated log or history buffer of every operation executed at a site (GROVE's Log, REDUCE's History Buffer), and it requires a scheme guaranteeing causal delivery order before any transformation is attempted — every algorithm here is layered on top of state-vector timestamping (attributed to GROVE) used specifically to ensure causality-p |
| `SUN-USENIXSEC-15` | J | RAPTOR: Routing Attacks on Privacy in Tor | A privacy tier that assumes symmetric routing observation is required to defeat a traffic-correlation attack is broken by any network topology exhibiting routing asymmetry, because an adversary needs only one direction of one of the two path segments (client-to-guard, exit-to-destination) to run the correlation attack described here, which halves the on-path coverage an adversary needs relative to |
| `TAHERIBOSHROOYEH-ARXIV-22` | I | WAKU-RLN-RELAY: Privacy-Preserving Peer-to-Peer Economic Spam Protection | Every routing peer must independently reconstruct and keep synchronized an identity commitment tree by observing the registration/removal event stream from the same smart contract, because the contract itself no longer stores the tree; a peer that falls behind risks proving membership against a stale root, which the paper states can expose that peer's leaf index and compromise its own anonymity. T |
| `TANG-ARXIV-22` | K | Stealthy Peers: Understanding Security Risks of WebRTC-Based Peer-Assisted Video Streaming | The demonstrated attacks depend on specific, named properties of the PDN designs studied, not on peer-assisted delivery generically: (1) service free riding requires the provider to authenticate customers with a long-lived, statically embedded API key rather than a short-lived, stream-bound token — the authors' proposed fix requires each token to carry a customer ID, peer ID, a specific video-ID l |
| `TANG-CCR-03` | B | pSearch: Information Retrieval in Structured Overlays | pSearch requires eCAN as its routing substrate specifically, not a generic DHT: the load-balancing, index-locality, and query-locality properties described above all depend on eCAN's bootstrap process being modified so a joining node's position is chosen from a document it is about to publish, which is a change to the DHT's join protocol, not an add-on above an unmodified DHT. Both pVSM and pLSI r |
| `THIEMT-CCS-25` | H | Generic Anonymity Wrapper for Messaging Protocols | Requires an underlying two-party or group messaging protocol that already derives fresh symmetric keys on a regular schedule (a continuous key-exchange protocol) — AW consumes these as its update-key input and does not itself generate long-term key material. Requires the sender and every receiver to advance their chain-key state in the same epoch sequence; a receiver that falls out of sync past th |
| `TRAN-NSDI-09` | F | Sybil-Resilient Online Content Voting | Requires a pre-existing trust network in which an attacker cannot easily obtain many attack edges; the paper states this as the sole requirement on the network's source (social graph, follow graph, etc.), without requiring a specific mixing-time bound beyond what its expander-graph proofs assume. Requires the vote collector to hold or be able to traverse the trust network to compute max-flow paths |
| `TRAUDT-ICDCS-21` | I | FlashFlow: A Secure Speed Test for Tor | FlashFlow requires the same Directory Authority (DirAuth) and Bandwidth Authority (BW Auth) infrastructure Tor already runs: DirAuths distribute the coordinator's public key in the network consensus, and a majority of DirAuths (and, in the paper's recommended configuration, their associated BW Auths) must be honest for the security bound to hold. It requires the BW Auths to hold sufficiently synch |
| `TRAUTWEIN-ARXIV-26` | L | Large-Scale Measurement of NAT Traversal for the Decentralized Web: A Case Study of DCUtR  | DCUtR requires that a relayed connection through a Circuit v2 relay already exists between the two peers before hole punching begins, so it depends on Circuit v2's own reservation and connection-establishment succeeding first (VYZOVITIS-SPECS-23); this paper's 29% prerequisite-failure figure is exactly the rate at which that dependency is not met. It requires the underlying transport to support bo |
| `TRAUTWEIN-INFOCOM-24` | A+J | IPFS in the Fast Lane: Accelerating Record Storage with Optimistic Provide | Every participating peer must implement the same threshold-based termination logic to be optimistically stored to during another peer's DHT walk, which the paper achieves by upstreaming the change into the shared go-libp2p-kad-dht implementation used by both the IPFS reference client Kubo and the Filecoin reference client Lotus; the paper states full backward compatibility is maintained, meaning a |
| `TRAUTWEIN-SIGCOMM-22` | C+J | Design and Evaluation of IPFS: A Storage Layer for the Decentralized Web | Content retrieval requires a functioning Kademlia DHT populated with enough long-lived DHT Server peers to answer two sequential lookups (CID-to-PeerID, then PeerID-to-Multiaddress) inside the 24-hour provider-record expiry window; if fewer than k = 20 peers close to a CID remain reachable, the provider record for that CID can be lost. Any consumer of IPFS retrieval-latency figures needs to accoun |
| `TYAGI-EUROCRYPT-22` | I | A Fast and Simple Partially Oblivious PRF, with Applications | Verification of a POPRF response requires the client to have the server's public key `pk` in advance and to run the Chaum-Pedersen zero-knowledge proof check on every response; without this check a malicious server can return an arbitrary value. Any protocol using tag-scoped tokens (for example a rate-limiting scheme selecting `t` as an autonomous-system number or time window) needs a mechanism, e |
| `TYAGI-SOSP-17` | G | Stadium: A Distributed Metadata-Private Messaging System | Stadium requires servers to be organized into mixchains such that servers within one chain belong to providers assumed unlikely to collude, and requires at least one server per chain to be honest and to follow the verifiable-shuffle protocol — the differential-privacy guarantee does not hold, and the paper does not claim it holds, if every server in some chain is adversarial. It requires two commu |
| `UNGER-SP-15` | H | SoK: Secure Messaging | As a systematization, this paper does not itself impose runtime requirements on a system; it states, as a finding rather than a design requirement, what other papers' mechanisms need. Its most load-bearing structural claim for downstream design is that the three problems (trust establishment, conversation security, transport privacy) are "nearly orthogonal" and their solutions "can largely be chos |
| `URDANETA-CSUR-11` | A | A Survey of DHT Security Techniques | The survey's own conclusion (section 7) states four preconditions the reviewed literature treats as jointly necessary for a secure DHT: secure assignment of node identifiers (an identifier a node cannot freely choose), a bound on the fraction of malicious nodes, malicious nodes spread across the identifier space rather than concentrated, and data replication combined with a routing mechanism that  |
| `VAJHA-FAST-18` | C | Clay Codes: Moulding MDS Codes to Yield an MSR Code | The storage system must support vector codes — multi-byte "superbytes" grouped per node — because Clay codes require sub-packetization level α > 1; the paper states plain Ceph did not support this and had to be modified (upstreamed into Ceph's master codebase) to add sub-chunking through interleaving. A repair operation needs the coordinator (in the paper's Ceph implementation, the primary OSD, "p |
| `VALAPU-ARXIV-25` | L | Towards a Non-Binary View of IPv6 Adoption | A design that treats "the client has IPv6" or "the destination has IPv6" as a fixed, binary property of a participant or service is contradicted by this paper's own measured variance: external IPv6 byte-fraction at a single residence has a daily standard deviation exceeding 15% (Table 1's per-residence daily standard deviation column ranges roughly 0.09-0.42 across residences), so any component th |
| `VANDENHOOFF-SOSP-15` | G | Vuvuzela: Scalable Private Messaging Resistant to Traffic Analysis | Vuvuzela requires at least one of the servers in its relay chain to be honest and to actually perform two functions in the protocol: generating that server's share of noise dead-drop accesses, and shuffling (mixing) the batch of requests before forwarding — the security proof (Section 6.1) is conditioned on this one-honest-server step existing, so any deployment or operational change that lets the |
| `VISHNUMURTHY-P2PECON-03` | I | KARMA: A Secure Economic Framework for Peer-to-Peer Resource Sharing | KARMA requires an underlying secure-routing DHT (the paper builds on Pastry with the Castro et al. secure-routing extension) that tolerates a bounded fraction of adversarial nodes and can map a node's hashed identifier to the k nodes closest to that hash; KARMA's own security bound against bank-set corruption holds only conditional on that routing layer's own bound holding. It requires every parti |
| `VISWANATH-EUROSYS-12` | F | Canal: Scaling Social Network-Based Sybil Tolerance Schemes | Canal requires the Sybil-tolerance scheme it accelerates to already express its access-control decision as a maximum-flow, or bounded-flow, query over a capacity-labeled graph — the credit-network abstraction the paper shows Ostra, SumUp, and Bazaar share; a scheme whose decision is not reducible to that form gains nothing from Canal. It requires every capacity change (an interaction consuming or  |
| `VISWANATH-SIGCOMM-10` | F | An Analysis of Social Network-Based Sybil Defenses | Any system built on one of these four schemes, or on community detection as a substitute, needs a designated trusted node (or trusted-node set) from which the ranking is computed, since every measured ranking here is defined relative to that node's position in the graph. It requires the input social graph to be undirected or convertible to an undirected weighted graph, since the paper's own conduc |
| `VORICK-SIA-14` | C | Sia: Simple Decentralized Storage | Every consumer of a storage proof needs only to read the blockchain, since proof validity is fully checkable from public data (the Merkle root committed in the contract, the revealed segment, and the accompanying hash path) — no direct interaction between client and host is needed for proof verification itself. The random-sampling proof-of-storage design requires a source of unpredictable randomne |
| `VYZOVITIS-SPECS-23` | L | Circuit Relay v2 (libp2p specification) | A private peer depending on Circuit v2 for reachability must maintain a live, continuously refreshed connection to at least one relay holding its reservation, because the specification states the reservation becomes invalid immediately if the reserving peer disconnects — so any component that assumes a private peer stays reachable through a relay across that peer's own connection churn is unsuppor |
| `W3C-DIDCORE-22` | E | Decentralized Identifiers (DIDs) v1.0 | A DID cannot be resolved or updated without a separate, independently specified DID method that defines the concrete verifiable data registry and the create/resolve/update/deactivate operations for that method; this specification supplies the identifier syntax and document data model the method's output must conform to, not the operations themselves. A DID resolver is required as a system componen |
| `W3C-VCDM-25` | E | Verifiable Credentials Data Model v2.0 | A credentialStatus check requires a separately specified and separately hosted status mechanism (the specification names `BitstringStatusList` only as an example) that the verifier fetches; that mechanism is required, by this specification's own normative language, not to let the issuer learn which specific individual's status a verifier is checking — ruling out any status design in which a verifi |
| `WACHS-CANS-14` | E | A Censorship-Resistant, Privacy-Enhancing and Fully Decentralized Name System | Resolution of any name outside a user's own zones requires a prior delegation the user (or one of their contacts, transitively) obtained out of band; the system supplies no mechanism for a first, trust-free discovery of an arbitrary unfamiliar zone. Publication of any record intended for other users requires a DHT that the paper explicitly treats as untrusted for integrity — every published record |
| `WALLEZ-SP-25` | H | TreeKEM: A Modular Machine-Checked Symbolic Security Analysis of Group Key Agreement in Me | Requires an untrusted Delivery Service external to TreeKEM that receives commit messages from every participant and rebroadcasts them to the rest of the group; MLS as analyzed here is explicitly not peer-to-peer — messages are not sent directly between participants. Requires that service, or some other external mechanism, to resolve the ordering of concurrent competing commits submitted by differe |
| `WALLEZ-USENIXSEC-23` | H | TreeSync: Authenticated Group Management for Messaging Layer Security | Requires a trusted Authentication Service (AS) external to TreeSync that attests to the binding between a member's real-world identity and their authentication credential, issued out-of-band before that member's leaf enters the tree — TreeSync's proof assumes this binding already holds and does not itself establish it. Requires a Delivery Service (DS) that stores and relays messages between partic |
| `WANG-ARXIV-26` | K | Whose Posts Get Ranked: Identical-Text Exposure Gaps in Bluesky Custom Feeds | The measurement method requires nothing from Bluesky's protocol beyond the already-public `app.bsky.feed.getFeed` endpoint returning a feed's current ranked Top-50 list; it requires no platform cooperation, no test-account creation, and no injected content, because it relies entirely on duplicate posts that already occur naturally on the platform. The fixed-effects fair-exposure audit method depen |
| `WANG-GLOBECOM-12` | J | Real-World Sybil Attacks in BitTorrent Mainline DHT | Any content-location or measurement system built on MLDHT that assumes a node's claimed identifier reflects a genuine distinct participant is false in this deployment, because MLDHT lets a node choose its own identifier and never requires the identifier returned in a PONG message to match the one requested in the preceding PING; a system safe against this needs an explicit consistency check betwee |
| `WANG-SIGCOMM-11` | L | An Untold Story of Middleboxes in Cellular Networks | Any peer-to-peer NAT-traversal scheme that assumes a client sits behind exactly one NAT box with one consistent mapping type is unsupported for the 3 of 72 carriers this paper measured assigning a client's connections to two different NAT boxes (with two different mapping types and sometimes two different external IP addresses) depending on a hash of the connection's five-tuple; a traversal scheme |
| `WANG-USENIXSEC-14` | G | Effective Attacks and Provable Defenses for Website Fingerprinting | The provable defense requires the client to determine, before or during a page load, which anonymity set (which shared supersequence) a load belongs to; the paper's four information levels bound what is realistic (full information, the strongest guarantee, requires knowledge of the load's entire future packet sequence in advance, which the paper states is "not generally of practical interest excep |
| `WEATHERSPOON-IPTPS-02` | C | Erasure Coding Vs. Replication: A Quantitative Comparison | The model requires a placement mechanism that distributes each block's fragments or replicas onto independently and uniformly randomly selected disks; the durability numbers are conditioned on that independence, which the authors state they cannot themselves guarantee without an unevaluated geographic-diversity mechanism from an overlay network such as CAN, Chord, Pastry, or Tapestry. The scheme r |
| `WEI-INFOCOM-12` | F | SybilDefender: Defend Against Sybil Attacks in Large Social Networks | Requires a single administrative party (or a coalition acting as one) with complete, current knowledge of the social graph's full edge set; the algorithm cannot run against a graph only partially visible to the party running it. Requires that party to already hold, from some external source, at least one node identity it is certain is honest, since the whole judge-node bootstrap in the pre-process |
| `WEI-NSDI-24` | A+J | The Eternal Tussle: Exploring the Role of Centralization in IPFS | A content-identifier-to-provider indexing layer that adds a centralized fast path (as the Indexer does) requires the content addressing scheme underneath it to be self-validating (a hash-derived content identifier) so that a compromised or malicious index operator cannot substitute false content, only false or withheld provider pointers — this paper's own security argument for Hydra-Booster safety |
| `WEI-PACMNET-25` | J+K | An Empirical Analysis of the Nostr Social Network: Decentralization, Availability, and Rep | A content-location or discovery layer consuming Nostr-style multi-relay replication needs a mechanism for a client to learn which relays hold a given user's or a given post's replicas, since the paper's own duplicate-retrieval measurement shows that without such a mechanism a client defaults to querying every relay it shares with an author, producing the measured 98.2% wasted-traffic outcome. A re |
| `WESOLOWSKI-EUROCRYPT-19` | I | Efficient Verifiable Delay Functions | The plain (non-trapdoor) VDF requires a public setup procedure that generates a group of unknown order without producing a known trapdoor; the paper states this is most efficiently done using the class group of an imaginary quadratic order rather than an RSA modulus, because RSA-modulus generation with a provably-unknown factorization either damages efficiency (public random sampling) or requires  |
| `WILCOX-OHEARN-STORAGESS-08` | C | Tahoe: The Least-Authority Filesystem | A reader needs to contact at least K of the N servers holding a file's shares to reconstruct it, and the freshness protocol additionally needs at least K + E servers to be reachable during the metadata-query phase for the staleness defense to function at all; a deployment with fewer than K+E reachable servers per file falls back to whatever subset it can reach, with the paper's stated freshness gu |
| `WILKINSON-STORJ-18` | C | Storj: A Decentralized Cloud Storage Network Framework | The Satellite must run continuously for repair to occur at all; a design that depends on Storj for durability therefore inherits an availability requirement on a specific centralized-per-user-or-organization process, not just on the storage-node population, which is a materially different assumption from a system whose repair logic runs from any node's local view (the paper's own comparison point  |
| `WILLIAMS-ARWEAVE-19` | C | Arweave: A Protocol for Economically Sustainable Information Permanence | A verifying or mining node must be able to fetch the specific deterministically-selected recall block for the current height on demand, so the network requires enough live nodes holding a spread of historical blocks that any given recall block is retrievable within the block-time window; the paper gives no quantified bound on how sparse blockweave replication could become before recall-block retri |
| `WILLOW-SPEC-23` | D | Willow: 3d Range-Based Set Reconciliation (protocol specification) | Reconciliation operates over `AuthorisedEntry` objects that already carry a `namespace_id`, `subspace_id`, `path`, `timestamp`, `payload_digest`, and `payload_length` — the rest of the system must supply and authenticate these fields before reconciliation can run, since the fingerprint function is defined over exactly this tuple. The protocol assumes each peer can enumerate, for an arbitrary `3dRa |
| `WOLCHOK-WOOT-10` | J | Crawling BitTorrent DHTs for Fun and Profit | A DHT that supports distributed content tracking by storing (info hash, peer address) pairs as key-value records cannot prevent a participant from reading those records once it holds a numerically nearby identity, because DHT replication delivers stored values to any node whose address is close enough in the keyspace, honest or not; the paper's central finding is that search-enabling replication a |
| `WORLDCOIN-WP` | E | World (Worldcoin) Whitepaper | A relying party that wants to check a PoH credential must be able to verify a Semaphore zk-SNARK proof (Groth16) against a public registry commitment; the paper assumes the verifier can read that on-chain (or state-bridged) commitment. The uniqueness check requires every enrollment to reach at least one live AMPC compute node operated by one of the named independent institutions (Nethermind, Unive |
| `XAVIER-ARXIV-24` | J | An evidence-based and critical analysis of the Fediverse decentralization promises | None — this is an argument-and-measurement paper about an existing deployed protocol's observed concentration, not a description of a mechanism with implementable preconditions for another system to satisfy. Its measured concentration figures (82% of instances under 5 active users; 50% of users on the top 20 of 21,146 instances) are directly relevant as a base rate against which any newly designed |
| `XIA-IMC-22` | E | Challenges in Decentralized Name Management: The Case of ENS | A ledger-based naming system that stores names as hashes (to bound identifier length and block enumeration during auction periods) requires an external plaintext-name dictionary — built from a wordlist, a domain list, or a data dump the naming project itself publishes — for any downstream service, moderation tool, or measurement pipeline to resolve a hash back to a human-readable string; absent th |
| `XIE-ARXIV-26` | L | Revisiting and Expanding the IPv6 Network Periphery: Global-Scale Measurement and Security | A component that assumes exposed-service prevalence tracks total device-population growth is contradicted by this paper's own longitudinal comparison: total exposure across the 15 original blocks dropped even as the device count in those same blocks grew roughly 5-fold, so a design must not infer growing attack surface directly from growing IPv6 deployment scale without an independent exposure mea |
| `XIONG-TKDE-04` | F | PeerTrust: Supporting Reputation-Based Trust for Peer-to-Peer Electronic Communities | The feedback-and-credibility trust computation requires an underlying distributed hash table (DHT) that maps a peer's identifier to designated storage peers in O(log N) hops; the paper's own prototype used P-Grid, but states Chord, Pastry, and CAN could substitute. The DHT must support a configurable replication degree (tested at r = 4) so that majority voting across replicas can detect a storage  |
| `XU-ARXIV-25` | B | Distributed Retrieval-Augmented Generation | Requires each peer to run a local LLM capable of two functions used inside the routing algorithm itself: extracting topics from the query text, and scoring semantic relevance between a returned knowledge snippet and the query, against threshold θ. The routing decision at every hop therefore depends on local LLM inference quality — the LLM-substitution results above show F1 varying by more than 6 p |
| `YANG-ICDE-03` | B | Designing a Super-Peer Network | The mechanism requires every client to maintain metadata synchronization with exactly one super-peer: on join, a client must transmit metadata for every file it holds; on any update (insertion, deletion, or modification of a shared item), it must resend the corresponding delta; and on leave, its super-peer must remove its metadata — without this discipline the super-peer's index (which is the sole |
| `YANG-IMC-11` | F | Uncovering Social Network Sybils in the Wild | A deployment of this detection approach needs a party that observes, for every account, its full inbound and outbound friend-request stream with timestamps and per-request accept/reject outcomes — data available only to the OSN operator itself in the studied deployment, not to any external observer or peer. Community-graph Sybil defenses (SybilGuard, SybilLimit, SybilInfer, SumUp) that this paper  |
| `YANG-SIGCOMM-24` | D | Practical Rateless Set Reconciliation | The transport between the two reconciling peers must preserve ordering of coded symbols, because the receiver reconstructs an implied index i for each coded symbol from position in the stream (after the sender transmits set size N with the 0th symbol) rather than the sender attaching an explicit index to each symbol; this is stated as an assumption for the variable-length count-field compression s |
| `YEN-EPRINT-26` | ? | BeeKEM: Decentralized, Secure and Efficient Group Key Agreement | BeeKEM requires a public key infrastructure (PKI) external to the protocol, treated as a black box, that lets a user obtain another user's initial public key before adding them to a group; the paper does not itself supply identity verification or key distribution. It requires a reliable broadcast protocol (RBP) — the paper's example is a gossip network all group members participate in — as the min |
| `YU-SIGCOMM-06` | F | SybilGuard: Defending Against Sybil Attacks via Social Networks | The mechanism requires an out-of-band process that establishes one edge per genuine, strong-trust human relationship and binds each edge endpoint to a public key; SybilGuard itself performs no identity verification and states explicitly it does not solve public-key distribution. It requires the social graph's honest region to be fast-mixing (mixing time Theta(log n)) — a topology violating this as |
| `YU-SP-08` | F | SybilLimit: A Near-Optimal Social Network Defense against Sybil Attacks | SybilLimit requires that the deployment's underlying trust graph be a social network in which the honest region is fast-mixing (O(log n) mixing time) and in which the number of attack edges g stays below o(n/log n); the guarantee degrades or fails outside that regime. Every participating node must maintain persistent per-instance routing-table state across multiple independent random-route instanc |
| `ZHANG-ARXIV-25` | K | Understanding Community-Level Blocklists in Decentralized Social Media | Community-level blocking in Mastodon requires no protocol-level mechanism beyond each instance administrator maintaining a local, unilateral list of domains to refuse; the paper's data shows this is enforced entirely at the instance level with no shared-state coordination required to block. Shared blocklist distribution requires either a synchronization tool (the paper names FediCheck and FediBloc |
| `ZHANG-FC-23` | A | Kadabra: Adapting Kademlia for the Decentralized Web | Each node must locally record, per query it routes or initiates, which peer the query passed through and how long a response took to arrive; the scoring function operates only on this locally observed per-peer latency history, so no external latency-measurement service or shared state across nodes is required, but every node must implement and continuously run this bookkeeping. The exploration mec |
| `ZHANG-INFOCOM-05` | K | CoolStreaming/DONet: A Data-Driven Overlay Network for Peer-to-Peer Live Media Streaming | Every node needs a stable, addressable identifier (the paper uses IP address) usable for the lifetime of the join process, and needs a persistently reachable origin node whose address is known in advance to bootstrap new joins. Buffer-map exchange assumes bounded and roughly known clock/segment-lag skew across nodes: the sliding-window size (120 segments in the prototype) is chosen on the basis of |
| `ZHANG-PACMHCI-24` | K | Trouble in Paradise? Understanding Mastodon Admin's Motivations, Experiences, and Challeng | This paper is an empirical measurement of a human cost, not a mechanism proposal, so it does not impose implementable requirements on other components. What it supplies is a target that any design replacing per-instance human moderators must account for: participants reported that moderation is currently done largely manually, is described as tedious and stressful, and exposes moderators to trauma |
| `ZHANG-TDSC-23` | F | Enhancing Sybil Detection via Social-Activity Networks: A Random Walk Approach | The mechanism requires a labeled seed set of honest users (and, for the sybil-seed extension, labeled sybil users) established by some process external to Sybil_SAN itself; the paper's own seed-selection procedure computes a credit-diffusion score over a derived "strong friendship" graph and takes the highest-credit users as honest until a target count (50 in the experiments) is manually verified, |
| `ZHAO-JSAC-04` | A | Tapestry: A Resilient Global-Scale Overlay for Service Deployment | Sybil resistance for the routing overlay's nodeID space is stated to require an external trusted public-key infrastructure that assigns node identifiers; Tapestry's own routing and repair mechanisms provide no defense against an entity obtaining many identifiers on their own. Confidentiality and message-integrity protection (message authentication codes, secure channels) are stated as supported at |
| `ZHONG-EMNLP-23` | B | Poisoning Retrieval Corpora by Injecting Adversarial Passages | Requires white-box or gradient access to the retriever's query and passage encoders to run the HotFlip token-replacement step; the paper's method computes gradients of the similarity score with respect to token embeddings, so a fully black-box retrieval service (no gradient access) is not directly attacked by the method as described — this bears on any content-location or search mechanism selected |
| `ZHOU-EPRINT-24` | B | Pacmann: Efficient Private Approximate Nearest Neighbor Search | - The graph the client searches must be built server-side with a fixed, uniform out-degree C for every vertex; an ANN index with irregular vertex degree (as ordinary HNSW or DiskANN indexes have) leaks information through the PIR access pattern and cannot be substituted without first regularizing it by the paper's build procedure. - The underlying retrieval primitive must be a client-preprocessing |
| `ZHOU-SP-24` | G | Piano: Extremely Simple, Single-Server PIR with Sublinear Server Computation | - A single server holds the full database in the clear; Piano's privacy guarantee is a property of client-server interaction, not of splitting the database across non-colluding servers, so anything that requires multi-server non-collusion assumptions (e.g., two-server PIR) is a different deployment model with different trust requirements. - The client must complete a one-time (and periodically rep |
| `ZIA-ARXIV-25` | K | Collaborative Content Moderation in the Fediverse | FedMod requires each participating instance to expose two API surfaces to peers: a Trends endpoint (`<instance>/api/v1/trends/tags`) returning weekly trending hashtags, used for hashsim similarity, and a way to serve model parameters to selected peers on request. It requires each instance to maintain its own labeled training set locally; no instance is required to disclose its raw labeled posts to |
| `ZIEGLER-ISF-05` | F | Propagation Models for Trust and Distrust in Social Networks | Appleseed requires every agent's trust statements to be retrievable on demand during trust-graph traversal (the paper's own implementation fetches each visited agent's machine-readable trust assertions "just in time" from a personal homepage), so the mechanism requires either a publishing convention for per-agent trust data (the paper adopts a FOAF-like public-homepage model) or an equivalent look |
| `ZOU-USENIX-25` | B | PoisonedRAG: Knowledge Corruption Attacks to Retrieval-Augmented Generation of Large Langu | The attack requires write access to the RAG system's knowledge database — an attacker who can insert texts that the retriever will later index and search. It requires the retriever to support a fixed similarity function between a question embedding and a text embedding (dot product in these experiments); the black-box variant needs no further access, but the white-box variant requires the attacker |
| `ZUO-ICWSM-24` | K | Understanding and Improving Content Moderation in Web3 Platforms | A per-user moderation signal of this kind (a mute) requires each mute to be attributable to a specific issuing user and a specific target user, and requires that record to be either directly observable (as on-chain data, in memo.cash's case) or otherwise obtainable in bulk for training — the mute-prediction classifier and the recommendation system are both trained on the full historical mute recor |
